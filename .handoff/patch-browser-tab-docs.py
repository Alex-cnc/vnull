# -*- coding: utf-8 -*-
'''一次性补丁：浏览器页签集成后的文档回填。跑完即删。'''
import pathlib
import sys

ROOT = pathlib.Path("/Users/alex/.dsh/projects/DoyahStudio")


def edit(rel, pairs):
    p = ROOT / rel
    t = p.read_text(encoding="utf-8")
    for label, old, new in pairs:
        n = t.count(old)
        if n != 1:
            print(f"❌ {rel} / {label}：{n} 次")
            sys.exit(1)
        t = t.replace(old, new, 1)
        print(f"✅ {rel} / {label}")
    p.write_text(t, encoding="utf-8")


# ---------------- 设计说明 ----------------
edit("Docs/design/浏览器页签-设计说明.md", [
    ("状态行",
     "| 状态 | 契约层 ✅（13 项测试）· 引擎适配 ✅（编译验证，运行时待手工清单）· 页签集成 ⬜ 待做 |",
     "| 状态 | 契约层 ✅（13 项测试）· 引擎适配 ✅（编译 + 5 项平台测试）· 页签集成 ✅ · **运行时待手工清单** · 会话持久化 ⬜ |"),
    ("§3 标题",
     "## 3. 待做二：页签模型集成（App 层）",
     "## 3. 页签模型集成（App 层）· 已完成"),
])

# ---------------- 需求书 ----------------
edit("Docs/需求规范书.md", [
    ("证据",
     "**仍待做**：页签模型集成（`EditorSelection` 方案见 `Docs/design/浏览器页签-设计说明.md` §3）",
     '''**2026-09-23 页签集成完成**：按方案引入了「两套页签并行 + 一个选中态」而不是把浏览器塞进 `QueryTab`
（`@Published var browserPages: [BrowserPage]` + `selectedBrowserID`；选中浏览器时编辑区显示浏览器，
`tabs` 与 SQL 路径**零改动**）；页签条渲染两段（浏览器在前、SQL 在后），菜单「新建浏览器页签」（⇧⌘B）；
引擎按页签缓存（切换 / 重绘不重新加载）；`BrowserTabView`（地址栏 + 后退前进刷新 / 停止 + 错误条 + 空白态）。
**修掉一个真问题**：出网记录原来写在游离的 `Task` 里 —— 既没有时序保证，也**漏掉了后退 / 前进 / 刷新**
（它们直接调 `view.load`，绕过了一切判定与记录）。现在导航接口异步化，**记完才走**，
且所有导航（地址栏 / 页内点击 / 脚本跳转 / 后退前进刷新）统一走 `navigate(to:)` 这一条通道；
`WKNavigationDelegate` 改用 async 变体。平台侧新增 5 项测试（含「`.other` 一律不算用户发起」这条
**安全属性**回归）。**仍待做**：① 页签状态随会话持久化（契约 ⑤ —— 需要先设计「已恢复但未加载」的状态
与显式加载入口，不该顺手糊一个会误导用户的行为）；② GUI 内按手工清单走一遍（我无法点击界面）'''),
])

CHANGELOG = '''| **v3.71** | 2026-09-23 | **FR-EDIT-34 页签集成完成**：引入「两套页签并行 + 一个选中态」（`browserPages` + `selectedBrowserID`），**不把浏览器塞进 `QueryTab`** —— 否则 SQL 专属字段会被污染成可选堆、约百处引用都要改成 `if let`，且每加一个浏览器字段就再污染一次；`tabs` 与 SQL 路径**零改动**。页签条渲染两段（浏览器在前），菜单「新建浏览器页签」（⇧⌘B），引擎按页签缓存。**修掉一个真问题**：出网记录原来写在游离 `Task` 里 —— 没有时序保证，且**漏掉后退/前进/刷新**（它们直接 `view.load`，绕过判定与记录）；现在导航接口异步化、**记完才走**，所有导航统一走一条通道，`WKNavigationDelegate` 改用 async 变体。平台侧新增 5 项测试（含「`.other` 一律不算用户发起」的安全属性回归）。**仍待做**：页签会话持久化（契约 ⑤，需先设计「已恢复但未加载」状态）、GUI 手工回归（我点不了界面）。`verify-core.sh` 808 + 平台 7 = **815 tests / 0 failures**；`verify-all.sh` 七项全过 | 鲸鱼娘（开发助理） |
'''
edit("Docs/需求规范书.md", [
    ("变更记录",
     "| 版本 | 日期 | 变更摘要 | 作者 |\n|---|---|---|---|\n",
     "| 版本 | 日期 | 变更摘要 | 作者 |\n|---|---|---|---|\n" + CHANGELOG),
])

# ---------------- 概要设计 ----------------
edit("Docs/概要设计.md", [
    ("§3.12 实现状态",
     "**引擎适配与页签集成见 `Docs/design/浏览器页签-设计说明.md`**" if False else
     "平台引擎适配（macOS 用 `WKWebView`）与页签集成待做，见 `Docs/design/浏览器页签-设计说明.md`。",
     "平台引擎适配（`WKWebView`）与页签集成**均已完成**（细节与手工回归清单见 "
     "`Docs/design/浏览器页签-设计说明.md`）；**仍待做**：页签会话持久化。"),
    ("变更记录 v2.4",
     "| 版本 | 日期 | 变更摘要 |\n|---|---|---|\n",
     "| 版本 | 日期 | 变更摘要 |\n|---|---|---|\n"
     "| **v2.4** | 2026-09-23 | **§3.12 浏览器页签集成落定**：选中态采用「两套页签并行 + 一个选中态」"
     "（`browserPages` + `selectedBrowserID`），**不把浏览器塞进 `QueryTab`** —— 避免 SQL 字段被污染成可选堆；"
     "页签条渲染两段、引擎按页签缓存、空白页不产生日志。**一条重要的实现级不变量**：所有导航必须走"
     "`navigate(to:)` 单一通道（记完再走），引擎的其他入口若绕过它就会漏记录 —— 回归测试里已含这条 |\n"),
])
print("\n📄 文档回填完成")
