# 智能数据库客户端 —— 产品能力规划说明书

| 项目 | 内容 |
|---|---|
| 文档编号 | **PC-CAP-001** |
| 版本 | **v1.0** |
| 日期 | 2026-09-23 |
| 文档位置 | `Docs/产品能力规划说明书.md` |
| 适用工程 | `DoyahStudio`（macOS 原生客户端，SwiftPM 包 `Package.swift`） |
| 文档性质 | **产品能力规划**：回答「为什么做 / 做什么 / 先做什么」。**不替代**需求规格（做成什么样、怎么验收）与概要设计（怎么实现） |
| 上游输入 | 需求提出者指示（2026-09-23）、《调研书-主流数据库客户端基础功能》、《调研书-AI智能体能力基线》、工程现状与差距分析 |
| 关联文档 | **PC-SRS-001** `需求规范书.md`（需求与验收）、**PC-HLD-001** `概要设计.md`（结构与实现）、PC-COMP-001 `兼容性矩阵.md`（版本实测）、`发布方案.md`、两份调研书 |
| 维护约定 | ① **能力状态不手工维护** —— 一律由 SRS 的状态列派生，本文件只给「域级判断」与「路线取舍」；② 每季度或每个里程碑复评一次，并在文末变更记录追加一行；③ 表格由 `Scripts/check-doc-tables.py` 校验 |

---

## 0. 三件套的分工（本文件在体系中的位置）

| 文档 | 回答的问题 | 主要读者 | 变更频率 |
|---|---|---|---|
| **产品能力规划说明书**（本文） | 为什么做、要具备哪些能力、先做哪个、为什么砍掉别的 | 产品决策 / 需求提出者 | 季度 / 里程碑 |
| **需求规格说明书**（PC-SRS-001） | 每条需求做成什么样、验收标准、状态与阻塞 | 开发 / 验收 | 每次需求变更 |
| **概要设计说明书**（PC-HLD-001） | 分层、模块、接口、并发与安全语义、ADR | 开发 / 后续维护者 | 架构变化时 |

**关系**：能力规划给出**能力域与优先级** → 需求规格把它落成**可验收的条目（FR / NFR 编号）** → 概要设计给出**实现结构**。
本文提到的能力若尚未在 SRS 中登记，一律标注「**待登记**」，不假装已有条目。

---

## 1. 产品定位与不可让渡的原则

**定位**：原生、轻量的数据库工作台；把**智能体**作为一等能力内置，但**默认关闭、每一步看得见、可审批、可审计、绝不自动执行**。

同一个产品做**两个平台实现**：macOS 版（本仓库）与 Linux 版（公司电脑），功能一致，只有平台与编程语言不同 —— 口径见 §1.1（P7）。

若与竞品（如 DataGrip 的 AI 能力、各类 MCP 数据库网关）比较，本产品的差异化**不在模型更强，而在「敢用」**：每一步都能被证明与追责。

| # | 原则 | 含义 | 依据 |
|---|---|---|---|
| P1 | **治理优先** | 显式同意、最小外发、权限继承当前连接、写操作逐次审批、审计留痕 —— **缺一条不上线** | NFR-AI-01 / 03 / 05、FR-AI-09、FR-AI-12；调研书结论「安全边界是协议层与产品层的硬约束」 |
| P2 | **可降级** | 无模型 / 未授权 / 断网时，**全部既有功能完整可用** | NFR-AI-02 |
| P3 | **本地模型优先 / 默认零外发** | 支持 OpenAI 兼容本地端点且免 Key；默认配置即「总开关关闭 + 端点为空」 | NFR-AI-06、NFR-AI-02 |
| P4 | **真实数据接地** | 诊断类能力必须站在真实执行计划 / 锁等待 / 统计之上，不做"没有依据的建议" | FR-DIAG-01、FR-DIAG-05、FR-SESS-01 |
| P5 | **可复核** | 智能体产出（SQL / 任务定义 / 建议）都以文本可查看、可编辑、可 diff | NFR-AI-05 |
| P6 | **原生轻量** | 无 JVM / 无 Electron；不为功能引入重量级运行时 | 1.2 产品目标 |
| P7 | **双平台等价** | macOS 与 Linux 是**同一个产品的两个实现**：能力清单、行为语义、验收标准一致；差异只允许出现在「平台适配层」以内，并且**逐条登记、可查、不可悄悄有** | SRS §0.8、§10.9、§10.10；概要设计 §3 平台适配层契约 |

---

## 1.1 双平台等价（P7 的落地口径）

### 1.1.1 什么算「一样」

| 维度 | 要求 | 判定方式 |
|---|---|---|
| **能力清单** | 两侧需求条目集合一致（数量与编号对应） | SRS §10.10 等价矩阵，由 `Scripts/gen-platform-parity.py --check` 强制 |
| **行为语义** | 契约级不变量一致：取消必须传到执行端且双向生效、受影响行数只报一次、结果截断必须可见、写操作逐次审批、密钥不入配置 / 审计 / 日志 | 概要设计 §3.2 / §3.10 的不变量清单；两侧各写等价契约测试 |
| **验收标准** | 同一条需求的判定标准两侧共用，不各写一套 | SRS 验收标准列（平台专属判定须在 §10.9 登记） |
| **交互与观感** | 设计令牌（间距 / 圆角 / 字号 / 表面与文本色 / 状态色 / 分类色）取值一致 | 概要设计 §3.9；两侧各有一份令牌表与对比度单测 |
| **文档** | 三书是**唯一共享事实源**，随代码一起进 Git，靠 GitHub 同步 | 本文 + SRS + 概要设计入库；`Scripts/verify-all.sh` 校验 |

**不算「一样」的部分**（允许不同，但必须登记）：菜单 / 快捷键的宿主约定、窗口与停靠行为、字体回退、
原生控件外观、安装与签名方式、终端仿真与 PTY、沙箱与文件授权机制、系统集成（通知 / 深色模式跟随）等。
每一处都进 SRS §10.9 平台差异登记表（P-01 ~ P-12），写清「为什么必须不同」与「用户可感知的差异」。

### 1.1.2 两侧的分工与禁止事项

- **契约向下、实现向上**：概要设计 §3 是两侧都必须满足的接口契约（`【契约】`）；平台实现细节标注 `【macOS】` / `【Linux】`，可自由替换。
- **禁止**：把一侧的库名、类型名、机制名写进需求条目 —— 需求描述必须在两侧都成立。工具会拦（`Scripts/check-platform-neutrality.py`，棘轮只降不升）。
- **禁止**：同一件事有两个真相。需求书 §3/§4 的「状态」列是 **macOS 基线**；Linux 状态只在 `Docs/平台实现状态.json` 登记。
- **禁止**：一侧加能力而另一侧不知情。新增 / 修改 `【契约】` 条目时，等价矩阵与差异登记表必须在同一次提交内更新。

### 1.1.3 当前状态与收敛路径

- **等价矩阵**：164 条需求全部登记在册（`Scripts/gen-platform-parity.py` 生成，改文档后自动核对）。
- **中立性**：**严格档已清零** —— 需求条目的「需求」列与 §1.2 / §1.3 正文里没有任何平台专属词（配额 0，`Scripts/check-platform-neutrality.py` 逐行守，负例已验证会拒绝）。**报告档 165 处**在「状态 / 证据 / 验收要点」列、§9 风险记录与 §10 附录里 —— 那些地方本来就在记 macOS 基线做了什么，**只统计不拦截**。两档口径写在 SRS §0.8：既不让实现细节当需求被照抄，也不逼人删实现记录。
  - 纠正一处我自己的口径错误：第一版脚本按"行"一刀切，把 190 处变更记录与 43 处证据列也算成负债（246 处），既收敛不到 0，也会逼人删掉有价值的实现记录。**是尺子不对，不是文档不对** —— 改按列归因后，真正混进需求正文的只有 25 处，现已全部改写为行为描述。
- **现状（2026-09-23 需求提出者明确）**：两台电脑**各做一个产品** —— 本机（macOS）做 DoyahStudio，另一台做**灵感收集器（鸿蒙）**。因此 **Linux 侧目前没有进行中的实现**；下面的契约（§0.8 / §10.9 / §10.10 与概要设计 §3）**保留待用**，但不要把它读成「正在排期」。真要开第二平台时，按这三份契约起手。
- **Linux 侧起步顺序**：先按概要设计 §3 落地适配层与三条自动门（见概要设计 §8.3），再做只读验证链路（连接 / 元数据 / 查询 / 结果网格 / 导出），能力对齐全靠 §10.10 矩阵收敛。

---

## 2. 能力地图（数据来源：SRS，2026-09-24）

14 个功能域、**166 条需求：112 ✅ / 46 🟡 / 8 ⬜**（数据源：SRS §10.1 索引，2026-09-24；含 FR-DRV-09 / NFR-COMP-05、FR-AI-13 ~ 15、**FR-EDIT-34（内嵌浏览器）+ NFR-SEC-08（统一外发日志）** 与本次新登记的 **FR-IO-07（CSV 导出编码：UTF-8 带 BOM / GB18030）**）。

| 能力域 | 一句话定位 | 总 | ✅ | 🟡 | ⬜ | 判断 |
|---|---|---|---|---|---|---|
| **FR-EDIT** SQL 编辑与执行体验 | 每天都要用，最该打磨 | 34 | 29 | 5 | 0 | **强** |
| **FR-CONN** 连接与网络 | 连不上就什么都做不了 | 20 | 14 | 5 | 1 | 中（SSH / 保活 / URL 导入仍缺） |
| **FR-EXEC** 执行与事务 | 正确性与可控性 | 17 | 15 | 2 | 0 | 强（事务开关 + 查询参数已接，待人工验证） |
| **FR-META** 元数据与对象树 | 数据库客户端的"导航" | 15 | 8 | 7 | 0 | 中（多数卡真机验收） |
| **FR-RES** 结果集 | 看数据的体验 | 14 | 7 | 7 | 0 | 中（域内已无未开始项） |
| **FR-AI** 智能体 | 差异化护城河 | 15 | 7 | 5 | 3 | **中（记忆层与治理已补齐）** |
| **FR-DRV** 驱动与方言 | 多库支持的地基 | 9 | 7 | 0 | 2 | 中（MySQL / GBase 驱动缺） |
| **FR-BLD** 构建与工程 | 可复现交付 | 9 | 9 | 0 | 0 | **强** |
| **FR-CLI** 命令行 | 无界面验证链路 | 5 | 5 | 0 | 0 | **强** |
| **FR-DATA** 数据浏览与编辑 | 从"看"到"改" | 6 | 2 | 4 | 0 | 中（内联编辑的界面 / 外键导航仍缺） |
| **FR-IO** 导入导出与备份 | 数据进出 | 7 | 2 | 4 | 1 | 弱（受 R-18 沙箱约束） |
| **FR-DDL** 表结构与 DDL | 结构变更 | 5 | 2 | 2 | 1 | 中（本轮补齐新建 / 改表） |
| **FR-DIAG** 性能与诊断 | 排查能力 | 5 | 3 | 2 | 0 | 中（统计面板缺） |
| **FR-SESS** 会话与服务器管理 | DBA 日常 | 5 | 2 | 3 | 0 | 中（Core + 真机脚本已交付，CLI 与界面待接） |

**读法**：✅ = 已实现；🟡 = **部分实现 / 已实现但待真机验收（混合桶）**；⬜ = 未开始。**🟡 不等于"没做"** —— 例如 FR-META-01 / 09 / 10 是 UI 与 Core 都完成、仅差真实库手工验收；FR-CONN-10 曾是「字段已存在、迁移逻辑未实现」，现已补上迁移器与读写两侧策略（✅）。

---

## 3. 差距的主题聚类（⬜ 8 项 + 🟡 中的真实缺口）

按"用户会怎么感受到缺失"聚类，而不是按编号：

| 聚类 | 缺口 | 典型条目 |
|---|---|---|
| **A. 连接工程化** | 连接多了没法管理；连不上内网；长连接被掐断 | FR-CONN-15/16/17/18/19/20 |
| **B. 结果集承载能力** | 大结果卡；不能排序 / 筛选 / 分页；复制格式单一 | FR-RES-07/08/09/10/12/14 |
| **C. 从"看数据"到"改数据"** | 不能就地改单元格；没有外键跳转 | FR-DATA-02/04/05/06 |
| **D. 结构变更与迁移** | 索引 / 约束界面、Schema 对比、ERD | FR-DDL-03（余下）/04/05 |
| **E. 诊断与观测** | 慢查询排行、库级统计面板 | FR-DIAG-03/04、FR-SESS-01/02/03 |
| **F. 数据进出（受沙箱约束）** | COPY 导入导出、备份恢复、Excel 互操作 | FR-IO-02/03/04/05/06（前置 R-18 / T-38） |
| **G. 多库驱动** | GBase 8a、MySQL 驱动与版本窗口 | FR-DRV-08/09、NFR-COMP-04/05 |
| **H. 智能体（本次重点）** | 见第 4 节 | FR-AI-03/04/07/10/11/12 |
| **I. 编辑器与效率** | 命令面板、主题字体、多光标、参数化查询 | FR-EDIT-25/26/27、FR-EXEC-17 |
| **J. 记忆与自学习**（本次新增聚类） | 跑过的 SQL 留不下、下次还得从头想；重复劳动不被沉淀 | FR-AI-13/14/15（见第 4.4 节） |

---

## 4. 智能体能力专项（本次扩展重点）

### 4.1 现状：内核已具备，缺"暴露 / 深度 / 复用"

已就绪的**治理内核**（均有单测）：接入配置与闸门（`AgentConfiguration` / `AgentGate`）、高危语句分类（`AgentGuardrail`）、逐次审批状态机与审计留痕（`AgentActionGate` / `AgentAudit`）、配额台账（`AgentQuota` / `AgentQuotaLedger`）、自然语言→SQL、specs→数据任务、合成数据。
**这意味着"治理"不是要新建的东西，而是已经存在的资产 —— 规划要做的是让它对外可用、能力更深、可复用。**

### 4.2 三档规划

**A 档｜旗舰（定义产品）**

| 能力 | 是什么 | 为什么闪 | 依赖 |
|---|---|---|---|
| **A1. MCP 作为统一工具接口**（先对内，后对外） | 内嵌智能体的每个动作都走 MCP 工具定义；之后同一套工具可对外暴露给外部智能体 | 与调研书结论一致：「以 MCP 作为对外与对内统一工具接口」。**每个工具调用天然过审批 + 审计 + 配额 + 最小外发** —— 竞品的 MCP 多是裸工具集 | FR-AI-10（🟡 已登记）；**对外暴露按调研书定为进阶可选** |
| **A2. Agent 技能包 + 项目级规则库** | 可版本化、可 diff、可审批的技能（SQL 评审规则、迁移检查表、数据质量探针、命名规范），落进项目目录 | 竞品刚上「AI Agent Skills」的是 IDE 内配置；我们的**技能本身进 git、可审计可回滚** | FR-AI-11（⬜）、工作区 Explorer（待登记） |
| **A3. 「证明再执行」** | 执行前自动：`EXPLAIN` 代价与全表扫描检查 → 事务内真跑再回滚 / `LIMIT 0` → 影响面（命中行数、级联、触发器）→ 输出可读结论，再进审批 | 竞品 AI 给 SQL，我们给 SQL **+ 证据** | 事务 API（FR-EXEC-11/15 🟡）是前置 |

**B 档｜强增益**

| 能力 | 说明 | 依赖 |
|---|---|---|
| **B1. 对话式诊断** | 把执行计划 / 锁等待 / 会话 / 统计喂给模型，产出"为什么慢 + 索引建议 + 反例"，并生成可审批的 `CREATE INDEX` | FR-AI-03（⬜）、FR-DIAG-01/05 已有 |
| **B2. 血缘与影响分析** | 由外键 / 视图 / 函数依赖推导"改这一列会波及什么"，迁移前给影响清单 | 元数据（FR-META） |
| **B3. 迁移成对生成** | 每个 forward DDL 必须配 **rollback**，先在影子 schema 验证再审批执行 | FR-DDL-03/04 |
| **B4. 对抗式复核（双 Agent）** | 一个出 SQL，一个**只找问题**（漏 WHERE、NULL 陷阱、隐式类型转换、锁风险），两份产出都进审计 | 纯编排，成本低 |
| **B5. 终端即工具（受审批）** | 智能体提议 `pg_dump` / `ssh -L`，摊开 → 批准 → 在**内嵌终端**执行 → 留痕 | FR-EDIT-29 已有；**R-18 / T-38 沙箱路线决定其可用性** |

**C 档｜打磨**

零外发可证明模式（外发日志核对）· 成本 / 配额面板与调用回放 · 合成数据安全试跑（FR-AI-07）· 项目级记忆（可检视、**绝不含行数据**）· specs 版本化重跑（FR-AI-11）。

### 4.3 与调研书结论的对应

调研书给的 MVP 必备项（工具调用 / schema grounding / 人在环显式同意 / 工具描述不可信 / 进度取消 / NL→SQL / 诊断 / 过度自主防护 / specs→任务 / 定时调度 / 沙箱导出 / 试运行预览）**在本工程已全部实现**。
本规划的三档**不重复 MVP**，只做调研书里「常用增强 / 进阶可选」的部分，并把其中两处**提前**（A2 技能包、A3 证明再执行）作为差异化。

### 4.4 记忆与自学习层（2026-09-23 讨论收敛，已登记为 FR-AI-13 ~ 15）

**要解决什么**：DBA 的查询工作是重复的 —— 同一批脚本翻来覆去地跑。客户端不该每次都让人从头想一遍：应当从「用户**实际跑过**的 SQL」里长出一条记忆，并**先把补全做扎实**，再谈参数化与主动建议。

**已收敛的四条决定**（需求提出者拍定，逐条已落进 SRS）

| # | 决定 | 说明 |
|---|---|---|
| 1 | **归档 `.sql` 要带元信息** | 元信息以**注释行**随文件保存（连接 / 库 / 首次与最近执行 / 次数 / 耗时 / 影响行数 / 结果 / 说明），语句本身原样内联。**元信息在注释里，所以「编辑器是纯 SQL 编辑器」与「SQL 带元信息」并不冲突** —— 这是本节一个被纠正过的误解：开发助理原先主张"元信息不该进 `.sql`"，实际 `Core/SQLArchive.render` 从 FR-EDIT-31 落地起就是这么写的 |
| 2 | **记忆必须按连接 / 环境隔离** | 记忆的键是连接（连接里已经含环境与登录账号），**每个登录用户有各自的记忆**：生产库的记忆不流进测试库，别人的记忆不流进我的 |
| 3 | **消费顺序：补全 > 参数化 > 主动建议** | 先把「你敲前几个字符就知道你要什么」做到位。**DBA 的重复是「同一批脚本反复跑」，不是「到点自动跑」** —— 定时需求主要在应用侧，所以主动那档排在最后 |
| 4 | **分类与生命周期** | 例行 vs 排障用四个统计量判定（见 H3）；生命周期**按记忆类型分**，不按固定天数一刀切 |

**事实源与派生索引（本节最关键的一条设计）**

归档文件既然是带元信息的、可读可执行的 `.sql`，它就是记忆层**唯一的事实源**；索引（指纹 / 频次 / 参数槽位）是**纯派生缓存，随时可从文件重建**。由此得到三个好处：

- 索引删掉**不丢任何数据**，重建即可 —— 让「索引这一层出错」变成无害的失败；
- **不需要另建侧车日志**，避免「文件和日志谁是真相」的经典纠缠；
- 用户可以用 grep / git 直接检视与版本化自己的记忆（与 P5 可复核同源）。

**流水线（S1 ~ S4）与现状**

| 阶段 | 做什么 | 现状 |
|---|---|---|
| **S1 采集** | 每次执行经 `recordHistory` 单一收口点落成带元信息的归档条目 | ✅ 已完成（FR-EDIT-31） |
| **S2 索引** | 从归档文件派生：粗 / 精指纹、频次、跨天分布、来源连接 | ⬜ 待做（FR-AI-13 的底座） |
| **S3 提炼** | 参数槽位识别、例行候选升格、技能条目生成 | ⬜ 待做（FR-AI-14） |
| **S4 消费** | 补全（第一优先）→ 参数化 → 主动建议 / 定时 | ⬜ 待做 |

> **S2 与「补全」不依赖 D1 ~ D6**：它们只需要本地文件与统计，**没有模型端点也能做、也能测**。真正需要模型端点的只有「由模型生成技能描述」与自然语言部分。

**已知缺口（决定暂不修）**：`SQLArchiveEntry` 只保留首次 / 最近执行时间与当天次数，**每次执行的时刻没有留**。判定「是不是每天 / 每周都跑」—— 跨天文件集合已经够；判定「是不是每天同一时间来、值不值得做成定时任务」—— 需要时刻。**已决定暂不加**，等 S4 确实需要定时建议时再补（改动有 `render → parse` 往返单测兜着）。

**四块硬骨头**（不解决，这一层就会长成「一个越记越乱的提示词堆」）

| # | 硬骨头 | 为什么难 | 倾向 |
|---|---|---|---|
| **H1** | **身份判定：参数化到什么程度仍算「同一条」** | 归一化太松会把语义不同的语句并成一条（补全给出错误候选）；太紧则同一查询碎成几千条（候选全是噪音）。`WHERE id = 1` / `= 2` / `IN (...)` 的列表长度差异怎么算 | 分两级指纹：**粗指纹**（结构骨架，用于聚类与频次）与**精指纹**（原文，用于「同一条」计数）；**补全用粗、计数用精**，两者不混用 |
| **H2** | **隔离边界：跨环境的通用知识怎么在不越界的前提下复用** | 强制隔离会丢掉「命名规范 / Oracle→PG 改写经验」这类与环境无关的知识；放开又会把生产库的库表名带到测试环境 | 记忆分两层：**环境层**（库表名 / 数据分布 / 这条在哪跑的 —— 严格隔离）与**通用层**（方言写法 / 命名习惯 / 改写经验 —— 可跨连接，但**不得携带具体库表名与数据值**，提升前过一道脱敏检查） |
| **H3** | **例行 vs 排障的判别** | 误判代价**不对称**：把一次性排障脚本当例行去定时是**危险**的（可能在生产上按时跑一条从没打算重复的语句）；把例行当排障只是没省事 | 四个统计量取保守侧：**跨天频次 / 跨越天数 / 时刻集中度 / 是否只改参数**。四条**全满足**才升格为「例行候选」，且**升格只产生建议、不自动建任务** |
| **H4** | **遗忘与纠错** | 记忆一旦自动写就会记错（一次误执行的探索性 SQL 会被当成习惯）；用户必须有「这条别记 / 删掉这条 / 为什么记了这条」的入口，且**立即生效** | ① 记忆**不直接写进长期层**，先进候选池、够门槛才升格；② 提供记忆浏览（原文 / 频次 / 来源连接）、单条删除、一键清空、「本次执行不记录」；③ 每次升格与遗忘都留一行**可读理由**（可解释），与 P5 同源 |

**与 Hermes memory 的关系（明确不照抄的地方）**

调研过 Hermes 的做法（`MEMORY.md` 平铺散文 + `§` 分隔、`memory_char_limit` 上限、`write_approval: false`、curator 到 `stale_after_days: 30` / `archive_after_days: 90` 就归档）后，**只借它的"分层记忆 + 定期整理"思路**，三处明确不抄：

1. **不抄 `write_approval: false`** —— 记忆自动落盘且无审批，与 P1 治理优先冲突；
2. **不抄按天数一刀切的生命周期** —— 「一年跑一次但很关键」的巡检脚本会被 30 / 90 天清掉，这正是 DBA 最不能被清的东西；
3. **不抄纯散文存储** —— 散文无法支撑「补全」这种需要结构化检索的消费，故必须有 S2 索引（散文可作为**可读摘要**共存在索引之上，而不是唯一载体）。

---

## 4.5 内嵌浏览器与统一外发日志（2026-09-23 拍定）

需求提出者在同一次对话里拍了两件事，**顺序本身就是决定**：

| 项 | 拍定内容 | 登记 |
|---|---|---|
| 统一外发日志 | **先做**。所有离开本机的请求（模型调用 / 浏览器导航与子资源 / 外部程序 / 更新检查）写进**同一份**可查看、可导出、可清空的日志 | **NFR-SEC-08** |
| 内嵌浏览器 | 做**完整浏览器**（不是只读预览），放在**编辑器区**（与 SQL 页签同级，**不是**下方面板里的一个页签） | **FR-EDIT-34** |

**为什么"先日志后浏览器"是对的**：浏览器是本产品**第一个通用出网入口** —— 在此之前，产品的
"默认零外发"可以靠"智能体总开关默认关闭"来保证；一旦能加载任意网页，这条承诺就需要**可观测**才能成立。
把外发日志放在前面，等于先给这条承诺装上计量表，再开门。见 R-35（浏览器的新攻击面与外发面）。

**行为契约（两侧一致，见需求书 §10.9 P-14）**：默认不加载远程内容、导航一律由用户显式发起、
出网全部进统一外发日志、独立数据存储（不共享 SQL 客户端的凭据与 Cookie）、下载走授权目录、
**不向页面注入任何客户端脚本桥**（不把数据库能力暴露给网页）。

**放在编辑器区而不是下方面板**的理由：下方面板在 FR-EDIT-30 定稿时被明确为**与产品未来解耦的应用级面板**
（问题 / 输出 / 终端 / 调试控制台四种通用页签），而浏览器是**任务性**的：
它服务于"边查资料边写 SQL"，与 SQL 页签同处一区才能并排、切换、随会话恢复。

---

## 5. 依赖、阻塞与前置决策

**这些不解决，下面的能力都排不上**：

| # | 事项 | 阻塞什么 | 状态 |
|---|---|---|---|
| D1 | **模型端点（本地或远端）+ API Key** | **所有**智能体能力的端到端验收（当前只有 Core + 假后端验证） | 待需求提出者提供 |
| D2 | **可达的 PostgreSQL 实例**（覆盖 12–15 / 17） | 4 项基线 🟡 的真机验收 | 部分有（16.2 / 18.6） |
| D3 | **GBase 8a / MySQL 实例**（MySQL 需覆盖多版本） | FR-DRV-08/09、NFR-COMP-04/05 | 都没有（见 T-60 / R-22） |
| D4 | **依赖选型**：`MySQLNIO`、MCP 相关库 | 多库驱动、A1 | 待需求提出者定（R-22） |
| D5 | **沙箱路线 T-38**（是否走 Developer ID 非沙箱分发） | FR-IO-04 备份恢复、B5 终端即工具 | 未定；本机已有非沙箱构建可验证 |
| D6 | **macOS 14 / 15 与 Intel 实机** | NFR-COMP-01 / 02 | 无机器 |

> **反向提醒**：第 4.4 节的记忆层 **S2（索引）与补全不在上表之内** —— 它只需要本地归档文件与统计，**不依赖 D1 ~ D6 的任何一项**，可以立刻开工。别让「等模型端点」把一件不需要模型的活拖住。

---

## 6. 建议的推进顺序

**Now（不依赖 D1～D6，马上能做且价值最高）**
1. 能力规划 → 需求登记的闭环：把第 4 节 A2 / A3 / B4 落成 FR-AI-13 起的条目（含风险与阻塞）
2. 「证明再执行」的 Core：`EXPLAIN` 判定 + 事务内试跑回滚 + 影响面结论（**纯逻辑、可单测**）
3. 对抗式复核的编排骨架（两次调用 + 审计留痕）
4. MCP 工具定义的内部抽象层（先对内，不引入依赖也能定义接口）
5. **记忆层 S2 + 补全**（FR-AI-13）：从归档 `.sql` 重建索引 → 编辑器补全。**纯本地纯统计，不依赖 D1 ~ D6**

**Next（拿到 D1 / D2 后立刻见效）**
5. 智能体端到端联调与验收（解锁全部 AI 条目的 ✅）
6. 对话式诊断 B1（需真实统计）、迁移成对生成 B3

**Later（外部条件具备后）**
7. 多库驱动（D3 / D4）、MCP 对外暴露 A1、终端即工具 B5（D5）、连接工程化 A 聚类、结果集承载 B 聚类

---

## 7. 能力 → 需求 → 设计的映射

| 能力（本文） | 已登记条目 | 待登记 |
|---|---|---|
| 智能体治理内核 | NFR-AI-01 ~ 06、FR-AI-09、FR-AI-12 | — |
| 智能体能力扩展 | FR-AI-03 / 04 / 07 / 10 / 11 | **A2 技能包、A3 证明再执行、B4 对抗式复核**（拟 FR-AI-16 ~ 18） |
| 记忆与自学习层（第 4.4 节） | **FR-AI-13 ~ 15（本次登记）** | — |
| 多库支持 | FR-DRV-08 / 09、NFR-COMP-03/04/05 | — |
| 工作区 Explorer | **FR-EDIT-32（2026-09-23 完成，✅）** | 智能体文件作用域（等模型端点）、工作区内 Git 与全文检索 |
| 外观与主题 | **FR-EDIT-33（2026-09-23 登记并实现）** | 手动选浅 / 深色主题（当前跟随系统）；外观方案的令牌层与逐屏替换 |
| 结果集承载 | FR-RES-07 ~ 14 | — |
| **双平台等价** | **P7；SRS §0.8 / §10.9 / §10.10；概要设计 §3（本次建立）** | Linux 侧适配层实现（另一仓库，按契约推进） |
| 统一外发日志 | **NFR-SEC-08（本次登记，先做）** | — |
| 内嵌浏览器 | **FR-EDIT-34（本次登记，等 NFR-SEC-08）** | 浏览器内直接操作数据库 / 页内 AI 助手（均未讨论，不登记） |

> 表中「待登记」项在写入 SRS 之前不视为已承诺的能力。

---

## 8. 复评机制

- **触发**：每个里程碑结束、或每季度一次；需求提出者提出新方向时立即复评。
- **动作**：① 用 SRS 重新派生能力地图（本文件第 2 节）；② 检查依赖表 D1～D6 有无变化；③ 调整三档顺序并在文末追加一行；④ 若某能力的**实现已完成但长期卡在验收**，在依赖表里标注，而不是留着假装在推进；⑤ **双平台视角复评**：核对 §10.10 等价矩阵与 §10.9 差异登记表 —— 新登记的能力是否两侧都排上了、新出现的平台差异是否登记了理由、中立性负债是否只降不升。
- **纪律**：**未实测的能力不得标记为达成**（与 NFR-COMP-03 / 05 同一纪律）；**未登记的差异不算差异，只算漂移**（P7）。

---

## 9. 变更记录

| 版本 | 日期 | 变更摘要 | 作者 |
|---|---|---|---|
| **v1.19** | 2026-09-24 | **按纪律重新派生能力地图（FR-IO-07 交付）**：166 条：**112 ✅ / 46 🟡 / 8 ⬜**（FR-IO 行 6 / 1 ✅ / 4 🟡 / 1 ⬜ → **7 / 2 ✅ / 4 🟡 / 1 ⬜**）。新增 FR-IO-07「CSV 导出文本编码可选（UTF-8 带 BOM / GB18030）+ 导入侧同一判决」并**一次做到底**（Core + 18 项单测 + CLI + 界面 + 真机脚本 16 项断言）。**关于「WPS 格式」的如实结论**：`.et` 是金山专有格式、无公开规范、Excel 打不开，本产品**不生成**它（登记 R-48 待决），改为把"WPS 能直接用"做实 —— `.xlsx` + `GB18030` 的 CSV，判据与手工步骤写在 `Docs/design/WPS-兼容导出说明.md`。数字由 `Scripts/check-doc-tables.py` 从 SRS §10.1 派生核对，不是手抄。 | 鲸鱼娘（开发助理） |
| **v1.18** | 2026-09-24 | **按纪律重新派生能力地图（FR-RES-14 交付）**：165 条：**111 ✅ / 46 🟡 / 8 ⬜**（FR-RES 行 7 ✅ / 6 🟡 / 1 ⬜ → 7 ✅ / 7 🟡 / 0 ⬜，**FR-RES 域已无未开始项**）。FR-RES-14「结果集导出 Excel（.xlsx）」由 ⬜ → 🟡：**用自写 xlsx 写入器解掉了「需第三方库选型」这个卡点**（ZIP + OOXML 自己写，22 项单测 + Python 独立解析验证）；代价如实登记：stored 不压缩、不能流式、单工作表，**Excel/Numbers 打开需人工**。数字由 `Scripts/check-doc-tables.py` 从 SRS §10.1 派生核对。 | 鲸鱼娘（开发助理） |
| **v1.17** | 2026-09-24 | **按纪律重新派生能力地图（FR-EDIT-27 交付）**：165 条：**111 ✅ / 45 🟡 / 9 ⬜**（FR-EDIT 行 29 ✅ / 4 🟡 / 1 ⬜ → 29 ✅ / 5 🟡 / 0 ⬜，**FR-EDIT 域已无未开始项**）。FR-EDIT-27「多光标与列编辑」由 ⬜ → 🟡：Core 多光标引擎（21 项单测）+ 编辑器接线（⌥⌘D / ⌥⌘↑↓ / ⌥ 拖拽列选择 / 多选区输入与退格，一次撤销）+ 菜单与帮助面板登记；**键位偏差已登记**：需求原文的 ⌘D 被「保存查询」占用，实际用 ⌥⌘D；**界面点击需人工**。数字由 `Scripts/check-doc-tables.py` 从 SRS §10.1 派生核对。 | 鲸鱼娘（开发助理） |
| **v1.16** | 2026-09-24 | **按纪律重新派生能力地图（FR-EDIT-26 交付）**：165 条：**111 ✅ / 44 🟡 / 10 ⬜**（FR-EDIT 行 29 ✅ / 3 🟡 / 2 ⬜ → 29 ✅ / 4 🟡 / 1 ⬜）。FR-EDIT-26「主题与字体」由 ⬜ → 🟡：主题三态（跟随系统 / 总是浅色 / 总是深色，与终端共用一份 Core 实现）+ 等宽字体族与字号可调（9–20 pt，不可用时回落系统等宽并如实提示）+ 外观面板新增两段；**观感需人工看一眼**。数字由 `Scripts/check-doc-tables.py` 从 SRS §10.1 派生核对，不是手抄。 | 鲸鱼娘（开发助理） |
| **v1.15** | 2026-09-24 | **按纪律重新派生能力地图（FR-SESS-03 交付）**：165 条：**111 ✅ / 43 🟡 / 11 ⬜**（FR-SESS 行 2 🟡 / 1 ⬜ → 3 🟡 / 0 ⬜）。FR-SESS-03 由 ⬜ → 🟡：Core（角色 / 表空间 / 扩展的浏览、解析、写语句生成与校验、风险分级）+ 45 项单测 + 真机脚本（本机 PG 16.2 真建改删一次性角色、装扩展再删、表空间只读列出）已交付；**CLI 与界面待接**，GBase 侧全部为离线推断（无实例）。数字由 `Scripts/check-doc-tables.py` 从 SRS §10.1 派生核对，不是手抄。 | 鲸鱼娘（开发助理） |
| **v1.14** | 2026-09-23 | **按纪律重新派生能力地图（状态同步 + 一处方法论修正）**：165 条：**110 ✅ / 21 🟡 / 34 ⬜**（FR-CONN 13→14 ✅、FR-EXEC 14→15 ✅）。触发这次重派生的是一件**我自己的账目错误**：需求提出者问「全部又都做完了？」，我去核才发现**正文定义行改了状态、§10.1 索引表没改** —— 而 `Scripts/report-requirements.py` 的盘点**读的正是索引表**，于是前几轮我给出的"完成度"一直是旧的（FR-CONN-10 / FR-EXEC-11 明明已完成）。**处置**：① 同步两处状态；② 新增 `Scripts/check-status-consistency.py` 并接进 `verify-all.sh`（现为 **8 项门禁**），从此「改了定义行忘了索引行」会在 CI 里直接失败；③ 顺带发现该脚本自己也被写错过两次（抓错列、状态整串比较），**都已改正并写成注释**。**教训**：盘点类数字必须由工具产出、且工具本身要能被反向校验 —— 手工维护的"完成度"与手工维护的状态一样，迟早漂移。 | 鲸鱼娘（开发助理） |
| **v1.13** | 2026-09-23 | **FR-EDIT-34 契约层完成（⬜ → 🟡）**，能力地图随之重派生：**165 条：108 ✅ / 23 🟡 / 34 ⬜**（FR-EDIT 行 1 🟡 → 2 🟡）。落地的是**契约层**（页面模型 / 导航策略 / 出网留痕流水线 + 13 项测试），引擎适配与页签集成仍在队列里；这一步的价值在于把「默认不加载远程内容」这条安全属性变成了**可单测的规则**，而不是散在 `WKWebView` 代理里的几行判断 | 鲸鱼娘（开发助理） |
| **v1.12** | 2026-09-23 | **记下分工现状**：两台电脑各做一个产品（本机 macOS 做 DoyahStudio，另一台做灵感收集器/鸿蒙），**Linux 侧没有进行中的实现**；§1.1 的双平台契约保留待用，§1.1.3 加「现状」一行以免被读成正在排期 | 鲸鱼娘（开发助理） |
| **v1.11** | 2026-09-23 | **统一外发日志主体完成（NFR-SEC-08：⬜ → 🟡）**，§4.5 拍定的顺序（先日志、后浏览器）走完第一步。落地要点见需求书 v3.65；对能力规划的意义：**「默认零外发」从口号变成可核对的事实** —— 面板空态即证明，导出可交给他人复核。下一步按同一节拍做 FR-EDIT-34（完整浏览器 / 编辑器区），它的出网全部接进这份日志后才算完成 | 鲸鱼娘（开发助理） |
| **v1.10** | 2026-09-23 | **内嵌浏览器与统一外发日志拍定（新增 §4.5）+ 评审高危缺陷修复后的能力重派生**。① 需求提出者拍定两件事，**顺序即决定**：先做**统一外发日志**（→ NFR-SEC-08），再做**完整浏览器**并放**编辑器区**（→ FR-EDIT-34）；② 新增 §4.5 记下拍定内容、顺序理由（浏览器是第一个通用出网入口，“默认零外发”从此需要**可观测**才能成立）、行为契约与「为什么放编辑器区而不是下方面板」（下方面板在 FR-EDIT-30 已明确为与产品未来解耦的应用级面板）；③ §7 映射表新增两行；④ 能力地图按 SRS 重派生：**164 → 165 条：108 ✅ / 22 🟡 / 35 ⬜**（FR-EDIT 33 → 34）。**同日代码侧**：评审确认的高危缺陷 R-29 / R-30 / R-32 / R-34 **已修复**、R-31 / R-33 **部分修复**（逐条写明未做部分）；**顺带测出文档漂移** —— 小节标题的「（N 条）」无人校验，§3.2 写 44 实际 50、§3.10 写 8 实际 9，已改正并加进 `check-doc-tables.py`。**verify-core.sh 760 → 776 tests / 0 failures；verify-all.sh 七项全过** | 鲸鱼娘（开发助理） |
| **v1.9** | 2026-09-23 | **确立双平台等价（新增原则 P7 + 第 1.1 节）并让三书入库**。需求提出者拍定：功能规划书 / 需求规格书 / 概要设计书**要上传 GitHub**，因为要在公司电脑上同步 Linux 版 —— 两个平台做**一样的功能**，只有平台与编程语言不同。① 定位从「macOS 原生」改为「原生」并写明两个平台实现；② 新增不可让渡原则 **P7 双平台等价**与落地口径 §1.1（什么算「一样」的 5 个维度 / 允许不同但必须登记的 9 类差异 / 4 条禁止事项 / 当前状态与收敛路径）；③ §7 映射表新增「双平台等价」行，§8 复评机制加双平台视角与「未登记的差异不算差异，只算漂移」；④ **三书 + `Docs/平台实现状态.json` 移出 `.gitignore`**（此前 Docs 几乎全被忽略，事实源不在版本控制里 —— 这是本次最实际的一处修补）；⑤ 新增 `Scripts/check-platform-neutrality.py`（**两档口径**：严格档 = 需求条目的「需求」列 + §1.2/§1.3 正文，**配额 0**；报告档 = 证据列 / 其他表格 / §9 / §10 附录，只统计不拦截。实测量化：第一版按行一刀切得 246 处，按列归因后真正混进需求正文的只有 25 处 → 全部改写为行为描述，**严格档 0 / 报告档 165**，负例已验证会拒绝）与 `Scripts/gen-platform-parity.py`（§10.10 等价矩阵 164 条，`--check` 幂等）；⑥ 两项与既有三项一起挂进 `Scripts/verify-all.sh`（四项 → 六项）。**本次仅文档与脚本：未改产品逻辑，`verify-core.sh` 760 tests / 0 failures** | 鲸鱼娘（开发助理） |
| **v1.8** | 2026-09-23 | **外观逐屏替换：热路径全部完成**。对象树、页签、工具栏、下方面板、连接列表全部令牌化（工具条原先自配的绿/红改走 `StatusTone`；新增 `TextTone.disabled` 与 `Theme.TextStyle.icon`；新增 **`CategoricalTone` 分类色**用于引擎徽标 —— 身份色与状态色分开，否则"橙色"既可表示引擎又表示告警）。**结果：热路径 0 处**（结果区 / 编辑器 / 对象树 / 连接列表 / 页签 / 工具栏 / 下方面板 / 外观面板 / 工作区面板 / 活动栏），剩余 **377 处全在功能面板**。**verify-core.sh 759 → 760 tests，0 failures** | 鲸鱼娘（开发助理） |
| **v1.7** | 2026-09-23 | **外观一致性核查 + 结果区补齐 + 防线第二次加宽**。需求提出者问「符合 theme 主题要求吗」，逐条核查后承认**上一轮的"结果网格完成"不准确**：表格本体迁了，**结果区外壳没迁**（系统文本样式 / 语义色 / 系统 Divider / 刻度外的 spacing）。已补齐 `ResultTableView`，并新增 `Overlay.Selection` / `Overlay.Zebra` 让"选中态与斑马纹的透明度"只定义一次（活动栏、侧栏、结果表同值），加单测守住"看得出但不糊"的区间。**校验脚本第二次加宽**：补上系统文本样式与语义前景色两类规则后，如实报出 `bare-textstyle` 240 + `bare-foreground` 129，待迁移总数 **92 → 460** —— **不是代码变差，是尺子变准**（前一版口径偏窄，我据它给出的进展数字偏低）。**verify-core.sh 756 → 758 tests，0 failures** | 鲸鱼娘（开发助理） |
| **v1.6** | 2026-09-23 | **外观逐屏替换：结果网格完成**。① 数值列右对齐 + 等宽数字的判定收在 `Core/ColumnAlignment`（白名单精确匹配，避开 `contains("int")` 把 `interval` 判成数值的陷阱，6 项单测）；② 结果网格全部改用令牌（行高 26、间距、字体分档、颜色、NULL 三档文本），**斑马纹 / 选中态 / 发丝线 / 表头自绘**（系统样式的颜色不受令牌控制，混着用就会"差一点点"）；③ 选中态用强调色淡填充 + 左侧 2pt 强调条，与活动栏 / 侧栏同一套语言；④ 复用单元格的外观每次重设，选中变化只重画变化的行。**补一个防线漏洞**：令牌校验原先漏掉系统语义色（`labelColor` / `separatorColor`），加宽后新增 4 处违规、基线 88 → 92（**主动放宽**，走 `--force` 路径并已实测"不加则拒绝"）。**verify-core.sh 750 → 756 tests，0 failures** | 鲸鱼娘（开发助理） |
| **v1.5** | 2026-09-23 | **FR-EDIT-32 收尾完成（🟡 → ✅）+ 逐屏替换开工**。① 归档默认目录跟随工作区（优先级收在 `Core/ArchiveLocation`：单独指定 > 工作区 > 没有），落在 `<根>/queries/`；② 工作区内按文件名有界搜索（忽略名单 / 深度 8 / 结果 200 / 不跟随符号链接），命中上限如实提示；③ **修掉上一轮的真缺陷** —— 树条目的相对路径按"被列出的那一层"而不是工作区根计算，导致**展开第二层会去错地方、列表空掉**（探针当时只展开一层所以漏过，现已补回归测试与两层展开的自检）；④ **外观逐屏替换开工**：编辑器语法色 / 诊断色改用令牌，棘轮基线 **94 → 88**；⑤ 能力地图重派生：**✅ 107 → 108、🟡 23 → 22**，映射表里工作区 Explorer 转为"完成"。**verify-core.sh 736 → 750 tests，0 failures** | 鲸鱼娘（开发助理） |
| **v1.4** | 2026-09-23 | **FR-EDIT-32 主体完成：活动栏 + 工作区 Explorer（⬜ → 🟡）**。① 活动栏（46pt 窄条）落在 `NavigationSplitView` 外面，选中态 = 左侧 2pt 强调条 + 图标提亮；**视图与动作分成两个类型**，因此"设置 / 账户"没有选中态；② 工作区 = 用户自选目录 + 沙箱授权书签（单独存 `workspace-bookmark.json`），文件树单层懒加载、符号链接只显示不跟随；③ **接入既有占位**：`TerminalWorkingDirectory.resolve(workspace:)` 的 `workspace` 此前一直传 `nil`，现在终端启动目录以工作区为准；④ 底部常显授权状态；⑤ **顺带修掉两个真缺陷** —— 非沙箱构建里安全作用域书签建不出来（改为两种模式兼容，见 ADR-19）、`DirectoryAccessStatus` 的文案是硬编码中文（补 `messageKey`）；⑥ 能力地图重派生：**⬜ 35 → 34、🟡 22 → 23**，映射表里工作区 Explorer 由"登记"推进到"主体完成"。**`verify-core.sh` 718 → 736 tests，0 failures；`verify-all.sh` 四项全过** | 鲸鱼娘（开发助理） |
| **v1.3** | 2026-09-23 | **强调色可配置（FR-EDIT-33，已实现）**。需求提出者指示「强调色做成可配置，鲸鱼蓝、深海青、鲸心品红都准备好」。① Core `AccentTheme` 三个候选（两个取自 `dsh-tui` 鲸鱼调色板）+ 解析回退 + WCAG 换算，11 项单测；② App `AccentManager` 持久化 + 根视图 `.tint` 下发，`AppearanceSheet` 画出真实效果（选中行 / 主按钮 / 焦点环）；③ **实测发现主色直接压白字不过 AA**（4.17 / 3.12 < 4.5），故每个候选分 `accent` / `fill` 两档，填充档实测 5.79 / 4.81 / 5.99；④ 能力地图重派生：**163 → 164 条：107 ✅ / 22 🟡 / 35 ⬜**，映射表新增「外观与主题」行。**`verify-core.sh` 693 → 704 tests，0 failures；两种构建均退出码 0** | 鲸鱼娘（开发助理） |
| **v1.2** | 2026-09-23 | **外观方案定调 + 新增 FR-EDIT-32（活动栏 + 工作区 Explorer）**。① 需求提出者看过样张后确认「这个界面比之前的好看多了」，定调为**混合**（浅色原生 / 深色专业）+ **紧凑但留白严格**，强调色暂用**鲸鱼蓝 `#4E6FFF`**（取自 dsh-tui 鲸鱼调色板 `B[78,111,255]`，改一行令牌即可更换）；② 外观落地路线：令牌层（零功能改动）→ 防回潮脚本 + 对比度单测 → 逐屏替换（结果网格 → 编辑器 → 对象树 → 页签 / 工具栏 / 状态栏 → 各功能面板）；③ 能力地图按 SRS 重派生：**162 → 163 条：106 ✅ / 22 🟡 / 35 ⬜**，映射表里「工作区 Explorer」由**待登记**转为 **FR-EDIT-32 已登记**；④ 外观与信息架构样张（含活动栏与工作区视图）见 `Docs/design/外观方案-v1.md`，渲染器 `Scripts/design-mock.swift` 带 50 项像素自检。**本次仅文档** | 鲸鱼娘（开发助理） |
| **v1.1** | 2026-09-23 | **新增第 4.4 节「记忆与自学习层」（讨论收敛后落档）**。① 记下需求提出者拍定的四条决定 —— 归档 `.sql` **要带元信息**（更正开发助理此前"元信息不该进 .sql"的错误判断：实际元信息一直是文件里的**注释行**，语句原样内联，二者不冲突）、记忆**按连接 / 环境隔离**（每个登录用户各有各的记忆）、消费顺序**补全 > 参数化 > 主动建议**（DBA 的重复是「同一批脚本反复跑」而非「到点自动跑」）、分类与生命周期按开发助理建议；② 立**事实源 = 归档文件、索引 = 纯派生缓存（可重建）**这条关键设计，并说明由此得到的三个好处（索引失效无害 / 不需要侧车日志 / 可 grep 可 git）；③ 画出流水线 S1 采集（✅ 已有）→ S2 索引 → S3 提炼 → S4 消费，并明确 **S2 与补全不依赖 D1 ~ D6**（已在第 5 节加反向提醒、第 6 节 Now 加第 5 条）；④ 摊开**四块硬骨头** H1 身份判定 / H2 隔离边界 / H3 例行与排障判别 / H4 遗忘与纠错，各给倾向；⑤ 记下**明确不照抄 Hermes 的三处**（`write_approval: false` 违反 P1；按 30 / 90 天一刀切会清掉「一年一次但关键」的巡检脚本；纯散文无法支撑结构化检索）；⑥ 能力地图按 SRS 重派生 —— 修正上一版的两处漂移（FR-EDIT-31 归档未计入、FR-EDIT 与 FR-AI 两行数字），**158 → 162 条：106 ✅ / 22 🟡 / 34 ⬜**，并新增差距聚类 **J. 记忆与自学习**；⑦ 映射表修正 Explorer 的拟编号（`FR-EDIT-31` 已被查询归档占用 → 拟 **FR-EDIT-32**），A2 / A3 / B4 的拟编号顺延为拟 FR-AI-16 ~ 18；⑧ 同步 SRS：登记 **FR-AI-13 ~ 15**（含 §10.1 索引、§10.2 追溯与 v3.49 变更记录）。**本次仅文档：未改产品逻辑，`verify-core.sh` 661/661 不变** | 鲸鱼娘（开发助理） |
| **v1.0** | 2026-09-23 | 首版。① 确立三件套分工（能力规划 / 需求规格 / 概要设计）与本文定位；② 固化 6 条不可让渡原则（治理优先 / 可降级 / 本地模型优先 / 真实数据接地 / 可复核 / 原生轻量）；③ 由 SRS 派生能力地图：14 域 158 条需求，105 ✅ / 22 🟡 / 31 ⬜，并对 9 个缺口主题聚类；④ 给出智能体能力扩展三档规划（A 旗舰：MCP 统一工具接口 / Agent 技能包 / 证明再执行；B 强增益：对话式诊断 / 血缘影响 / 迁移成对生成 / 对抗式复核 / 终端即工具；C 打磨），并**与《调研书-AI智能体能力基线》的结论对齐**（不重复其 MVP 必备项，只做常用增强与进阶可选，并把技能包与"证明再执行"提前作为差异化）；⑤ 列出 6 项前置决策与阻塞（模型端点、实例、依赖选型、沙箱路线、实机），明确"未解决则排不上"；⑥ 给出 Now / Next / Later 顺序与「能力 → 需求 → 设计」映射，未登记项明确标注**待登记**；⑦ 规定每季度 / 里程碑复评与"未实测不得标达成"的纪律 | 鲸鱼娘（开发助理） |
