# 测试用例

| 项目 | 内容 |
|---|---|
| 文档编号 | PC-TEST-001 |
| 版本 | v1.2 |
| 日期 | 2026-09-21 |
| 关联文档 | `Docs/需求规范书.md`（UC-01 ~ UC-20、AC-REL-01 ~ 08）、`Docs/兼容性矩阵.md`、`Docs/GBase-技术验证.md` |
| 维护约定 | 用例编号只增不删；新增功能同时补「自动化」列（单测 / 链路脚本 / 手工）；表格改动后运行 `python3 Scripts/check-doc-tables.py` |

**自动化图例**：`🤖 单测` = `./Scripts/verify-core.sh`；`🔗 链路` = `./Scripts/test-local-query-path.sh`；`🖐 手工` = 需要真实库或 UI 交互；`📦 版本` = `./Scripts/test-postgres-version-matrix.sh`。

---

## 1. 自动化回归基线

| 脚本 | 覆盖 | 当前结果 |
|---|---|---|
| `Scripts/verify-core.sh` | 拆分 / 方言 / 配置 / 静态检查 / tokenizer / 元数据映射 / 保存查询 / 本地化 / 格式化 / 页签编号 / 结果导出 / 影响行数判定 / 编辑器文本同步 / 建库权限 | ✅ 89/89 |
| `Scripts/test-local-query-path.sh` | CLI → 拆分 → 驱动 → 本地 PostgreSQL 全链路（含 DML 影响行数、服务端取消、对象树、跨库、建库权限与建库） | ✅ 28/28 |
| `Scripts/test-postgres-version-matrix.sh` | 对任意 PG 实例跑 5 类用例并输出矩阵行 | ✅ 16/16（16.2 本地） |
| `Scripts/build-app.sh` | App 编译 + 打包 + ad-hoc 签名 | ✅ 可运行 |

---

## 2. 功能用例（按 UC 展开）

### 2.1 连接管理（UC-01 ~ UC-03、FR-CONN-01 ~ 14）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-01 | 新建连接并测试成功 | 已有可达 PostgreSQL | 侧边栏「+」→ 填名称 / 主机 / 端口 / 库 / 用户 / 密码 → 点「测试连接」 | 显示 `server_version / database / user`；无错误横幅 | 🖐 |
| TC-02 | 测试失败提示可读 | 同上，密码故意填错 | 同上步骤，密码改为错误值 | 横幅显示 `PSQLError` 详情（含认证失败），不崩溃 | 🖐 |
| TC-03 | 端口越界不可保存（FR-CONN-06） | 打开新增表单 | 端口填 `70000` / `abc` / `0` | 「测试连接」「保存」均禁用；填回 1–65535 后恢复可用 | 🖐（模型规则由 TC-31 覆盖） |
| TC-04 | 必填项为空不可保存 | 打开新增表单 | 清空名称 / 主机 / 用户名 | 按钮禁用 | 🖐 |
| TC-05 | 编辑连接（密码留空不改） | 已保存连接 | 右键「编辑」→ 修改名称与主机 → 保存 → 重新打开编辑面板 | 字段回填新值；密码区显示「留空则不修改」；连接仍可用 | 🖐 |
| TC-06 | 删除连接二次确认（FR-CONN-05） | 已保存连接 | 右键「删除」 | 出现确认对话框，文案明示「同时删除钥匙串密码且不可撤销」；点「取消」连接仍在 | 🖐 |
| TC-07 | 删除后清理彻底 | 同上 | 确认删除 → 查看钥匙串（Keychain Access 搜 `com.vnull.PostgresClient`）→ 重新添加同名连接 | 列表中消失；Keychain 无残留密码项；重新添加可正常连接 | 🖐 |
| TC-08 | 记忆上次选中连接（FR-CONN-11） | ≥2 个已保存连接 | 选中第 2 条 → 退出 App → 重新打开 | 仍选中第 2 条 | 🖐 |
| TC-09 | 被删除连接不残留选中项 | 选中某连接后删除它 | 删除 → 退出 → 重新打开 | 选中项回退到列表首条，不出现悬空选中 | 🖐 |
| TC-10 | 服务器 / 数据库上下文栏（FR-CONN-12 / 13） | 已连接 | 顶部选服务器 → 打开数据库下拉 → 选另一个库 → 执行 `SELECT current_database();` | 下拉只列出有 CONNECT 权限的库；结果等于所选库 | 🖐 + 🔗（库列表） |
| TC-11 | 切换服务器刷新对象树 | ≥2 个连接 | 切换服务器 | 对象树重新加载：根节点、数据库列表、schema 均对应新服务器 | 🖐 |

### 2.2 查询执行（UC-04 ~ UC-07、UC-12、FR-EXEC-01 ~ 13）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-12 | 执行单条查询 | 已连接 | 输入 `SELECT 1 AS one, now();` → 点执行（或 ⌘↩） | 结果表格显示 1 行；状态栏显示语句数与耗时 | 🔗 |
| TC-13 | 中文与 NULL 往返 | 已连接 | `SELECT '中文' AS c, NULL AS n;` | 中文正确；NULL 显示为 `NULL` | 🔗 |
| TC-14 | 多语句与多结果集（UC-05） | 已连接 | 执行 3 条语句（建表 / 插入 / 查询） | 状态栏「3 条语句」；结果区下拉可切换结果集 | 🔗 |
| TC-15 | 字符串内分号不拆句 | 已连接 | `SELECT 'a;b' AS s;` | 只有 1 条语句，结果为 `a;b` | 🔗 |
| TC-16 | 美元引号函数体不拆句 | 已连接 | 创建 `$$ … ; … $$` 函数并调用 | 2 条语句；函数返回正确 | 🔗 |
| TC-17 | DML 影响行数（FR-EXEC-10） | 已连接 | 执行 `INSERT`（3 行）/ `UPDATE`（2 行）/ `DELETE`（1 行） | 状态栏分别显示影响 3 / 2 / 1 行；结果区显示「影响 N 行」 | 🤖 + 🔗 |
| TC-18 | 无结果集语句提示 | 已连接 | 执行 `CREATE TABLE t (...)` | 显示「没有返回结果集」；不产生空表格 | 🔗 |
| TC-19 | 中途取消（FR-EXEC-08） | 已连接 | 执行 `SELECT pg_sleep(30);` → 点「停止」 | 1–2 s 内结束；错误信息含 `canceling statement due to user request`；页签恢复可用 | 🤖（本地链路用例 7）+ 🖐 |
| TC-20 | 执行中按钮状态互换（FR-EXEC-13） | 已连接 | 默认态观察 → 点执行 → 观察 → 执行结束 | 默认：执行=暗绿可点、停止=灰不可点；执行中：执行变灰、停止变红；结束后恢复 | 🖐 |
| TC-21 | SQL 错误可读 | 已连接 | `SELECT * FROM no_such_table_xyz;` | 状态栏「执行失败」+ 错误详情含表名；连接仍可用；CLI 退出码 2 | 🔗 |
| TC-22 | 空 SQL 不执行 | 已连接 | 清空编辑器 → 点执行 | 提示「SQL 为空」，不发起请求 | 🖐 |
| TC-23 | 语法预检（UC-07） | 已连接 | 点「检查」对 `SELECT * FROM 不存在的表`；再对 `UPDATE t SET x=1` | 前者报错（EXPLAIN 失败）；后者只做计划、不落库 | 🖐 |
| TC-24 | 语法静态检查 | 编辑器可用 | 输入 `SELECT 'abc`（未闭合）与 `SELECT (1` | 出现红色点状下划线 + 诊断条提示 | 🤖（SQLLinter 15 项）+ 🖐 |
| TC-25 | 执行历史回填（FR-EDIT-10） | 已连接并执行过 ≥2 条查询 | 打开工具栏时钟菜单 → 点击历史项 | 历史按时间倒序（✓ / ✗ 标记）；点击后 SQL 回填当前页签；「清空历史」可用 | 🖐 |
| TC-26 | 历史不落盘（DR-02） | 执行若干查询 | 退出 App → 检查容器内文件 → 重开 | 沙箱容器内**没有**历史文件；重开后历史为空 | 🖐 |

### 2.3 结果与导出（UC-09、FR-RES-01 ~ 06）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-27 | 结果复制为 TSV | 有结果集 | 选中 3 行 → ⌘C → 粘贴到文本编辑器 | 3 行 × N 列，制表符分隔，含表头 | 🖐（UC-09 待补齐，见 R-04） |
| TC-28 | 导出 CSV（FR-RES-06） | 有结果集（含中文 / NULL / 逗号 / 引号） | 结果区右上角导出菜单 → CSV → 选路径保存 | 文件为 UTF-8（带 BOM）、CRLF 行尾、特殊字符被正确转义、NULL 导出为空字段 | 🤖（10 项）+ 🖐 |
| TC-29 | 导出 JSON | 有结果集 | 同上 → JSON | 结构含 `columns / rows / rowCount / affectedRows`；重复列名有后缀；NULL 为 `null` | 🤖 + 🖐 |
| TC-30 | 无可导出数据提示 | 执行 `CREATE TABLE …` 后 | 点导出菜单 | 提示「没有可导出的数据」 | 🖐 |
| TC-31 | 取消导出不产生文件 | 有结果集 | 打开导出面板 → 取消 | 无文件生成，状态栏不变 | 🖐 |

### 2.4 编辑器与文件（UC-17 ~ UC-20、FR-EDIT-01 ~ 24）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-32 | 打开 SQL 文件为新页签（FR-EDIT-15） | 准备 `.sql` 文件 | 工具栏「打开文件」→ 选择文件 | 新页签标题 = 文件名；内容正确；不绑定连接；可直接编辑 | 🖐 |
| TC-33 | 打开非文本文件被拒绝 | 准备 `.pdf` / `.rtf` | 同上 | 给出可读错误，不产生页签 | 🖐 |
| TC-34 | 保存 / 另存为（FR-EDIT-16） | 打开的文件页签 | 编辑 → ⌘S（主体保存）→ 再点下拉箭头「另存为…」 | 首次写回原路径；另存为生成新文件并更新标题；未保存标记清零 | 🖐 |
| TC-35 | 未保存标记 | 任意页签 | 编辑内容 | 标题出现「•」；保存后消失 | 🖐 |
| TC-36 | 编辑菜单六项（FR-EDIT-17 ~ 22）（UC-19） | 编辑器有内容 | 依次使用查找 / 替换 / 跳到行列 / 缩进 / 反缩进 / 清除 / 格式化 | Find Bar 可用；跳转定位准确；缩进为 4 空格；清除可 ⌘Z 撤销；格式化只改大小写与换行，标识符不变 | 🤖（格式化 4 项 + 跳转手工）+ 🖐 |
| TC-37 | 补全（FR-EDIT-09） | 编辑器输入 `SEL` | F5 / Esc / ⌃Space | 弹出候选（`SELECT` 等），插入后高亮正常 | 🖐 |
| TC-38 | 页签编号单调递增（FR-EDIT-24）（UC-20） | 初始 1 个页签 | 新建 2 个（查询 2、查询 3）→ 关闭「查询 2」→ 再新建 | 新页签为「查询 4」（不回退、不复用） | 🤖（3 项）+ 🖐 |
| TC-39 | 清除查询后执行 | 编辑器有内容 | 编辑菜单「清除查询」→ 点执行 | 提示 SQL 为空；诊断与检查结果同步清空 | 🖐 |
| TC-40 | 编辑区 / 结果区拖拽（FR-EDIT-13）（UC-15） | 已执行查询 | 拖动中间分隔条 | 两区高度实时变化；最小高度 120 pt 不被压破 | 🖐 |

### 2.5 对象树（UC-08、FR-META-01 ~ 11）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-41 | 层级下钻 | 已连接 | 展开 服务器 → 数据库 → schema → 表 → 列 | 各层正确；列显示类型；任意层级可继续展开 | 🤖（8 项元数据）+ 🔗 + 🖐 |
| TC-42 | 空 schema 提示 | 库中有空 schema | 展开该 schema | 显示「该 schema 下暂无表 / 视图」，不报错 | 🔗 |
| TC-43 | 系统 schema 不出现 | 已连接 | 展开 `pg_catalog` 所在库 | 系统 schema 被过滤 | 🔗 |
| TC-44 | 跨库浏览（FR-META-10） | 服务器上有 ≥2 个库 | 展开非当前库 | 按需建立该库独立连接并列出其表；单库失败不影响其它库 | 🔗 + 🖐 |
| TC-45 | 无权限库不出现 | 权限受限账号 | 展开服务器节点 | 只列出当前用户可连接的库 | 🖐 |
| TC-46 | 元数据失败可重试 | 断开网络 | 展开对象树 → 点刷新 | 显示可读错误；网络恢复后点「重试 / 刷新」可加载成功 | 🖐 |
| TC-67 | 服务器节点右键菜单（FR-META-11） | 已保存连接，对象树已加载 | 右键「服务器」节点 | 菜单含「连接」「断开」「编辑连接…」；已连接时「连接」置灰，未连接时「断开」置灰；「编辑连接…」打开连接表单且字段回填 | 🖐 |
| TC-68 | 建库入口按权限呈现（FR-META-11） | ① 超级用户 / 有 `CREATEDB` 的账号 ② 无建库权限的账号 | 右键服务器节点，对比两种账号下的菜单 | ① 出现「新建数据库…」；② **不出现**该项（权限探测失败或方言不支持时同样不出现）；菜单不出现灰色死按钮 | 🤖（三态解析 3 项）+ 🖐 |
| TC-69 | 新建数据库并入树（FR-META-11） | 有建库权限的账号 | 右键服务器 → 新建数据库… → 输入 `ic_menu_test` → 创建 | 名称非法时「创建」禁用并提示；创建成功后状态栏提示、数据库下拉与对象树服务器节点下出现 `ic_menu_test`；重复创建报服务端错误且提示可读 | 🔗（探测 + 建库 + 树）+ 🖐 |
| TC-70 | 断开 / 重连语义（FR-META-11） | 已连接且对象树已展开到表 | 右键 → 断开 → 再右键 → 连接 → 展开任意 schema | 断开后状态栏提示、连接状态翻转；对象树保留已加载内容；重新连接后可继续展开并刷新 | 🖐 |

### 2.6 保存查询、国际化、帮助（FR-EDIT-14、NFR-I18N-03、FR-EDIT-23）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-47 | 保存查询并回填（UC-14） | 有 SQL | 工具栏书签 → 命名保存 → 重启 App → 已保存列表 → 点击条目 | 保存成功（重名提示覆盖）；重启后仍存在；点击回填当前页签 | 🤖（3 项 store）+ 🖐 |
| TC-48 | 已保存查询不含密码 | 已保存若干查询 | 打开容器内 `saved-queries.json` | 只有 id / name / connectionID / database / sql / savedAt，无密码字段 | 🤖 + 🖐 |
| TC-49 | 语言切换（UC-16） | 任意界面 | 菜单「语言 / Language」→ English → 逐页检查 → 重启 → 切回中文 | 文案与日期格式立即切换；无中文残留；重启保持；切回无英文残留 | 🤖（6 项 key 覆盖）+ 🖐 |
| TC-50 | 帮助占位（FR-EDIT-23） | 任意界面 | 点问号按钮 | 显示「帮助内容待补充」，不崩溃 | 🖐 |
| TC-64 | 中文输入法提交后光标不跳（BUG-005 回归） | 编辑器非空或为空皆可，系统输入法为拼音 / 五笔 | 切到中文输入法 → 输入几个字（组字中）→ 回车提交 → 继续输入英文字符 | 英文字符出现在**光标位置**（串尾），不会插到字符串开头；中文内容完整、无丢失；提交后再切换输入法仍正常 | 🖐（判定逻辑由 `EditorTextSyncTests` 8 项覆盖） |
| TC-65 | 连接显示名含登录用户名（FR-CONN-14） | 已保存连接（名称 `PGConn`，账号 `postgres`） | 观察侧边栏连接行 → 打开服务器选择器 → 右键删除（只看文案后取消） | 三处均显示 `PGConn (postgres)`；名称为空时显示「未命名 (账号)」；账号为空时不出现空括号 | 🤖（3 项）+ 🖐 |
| TC-66 | 外部改动不被编辑器吞掉（BUG-005 相关回归） | 已保存查询 / `.sql` 文件 | 在编辑器输入若干内容 → 从「已保存列表」载入另一条查询 → 再从文件打开页签 → 切回原页签 | 载入后编辑器内容立即变为目标 SQL（光标回到开头）；页签切换互不串内容；⌘Z 不会把页签内容换回上一个页签的内容 | 🖐（判定逻辑由 `EditorTextSyncTests` 8 项覆盖） |

### 2.7 命令行（UC-11、FR-CLI-01 ~ 05）

| 编号 | 用例 | 前置 | 步骤 | 预期 | 自动化 |
|---|---|---|---|---|---|
| TC-51 | CLI 单条 SQL | 可达库 | `PGHOST=… ./PostgresClientCLI -c "SELECT 1;"` | 打印连接信息、结果、语句数与耗时；退出码 0 | 🔗 |
| TC-52 | CLI 对象树 | 同上 | `--tree --columns` | 按层级缩进打印；跨库可展开 | 🔗 |
| TC-53 | CLI 错误退出码 | 同上 | `-c "SELECT * FROM no_such_table;"` | 退出码 2；错误详情含表名 | 🔗 |
| TC-54 | CLI 服务端取消 | 同上 | `--cancel-after 1 -c "SELECT pg_sleep(30);"` | 约 1 s 后取消，退出码 2，错误含 `canceling statement due to user request` | 🔗 |

---

## 3. 非功能用例

| 编号 | 用例 | 方法 | 预期 | 自动化 |
|---|---|---|---|---|
| TC-55 | 1 万行 × 20 列渲染 | 造数据后执行查询并滚动 | 无可感卡顿（NFR-PERF-02） | 🖐 |
| TC-56 | 10 万行不 OOM | `maxRows` 截断后执行 | 不崩溃；内存可控（NFR-PERF-03，见 R-03 待改进） | 🖐 |
| TC-57 | 元数据懒加载时延 | 局域网库展开对象树 | 单层 ≤ 1 s（NFR-PERF-04） | 🖐 |
| TC-58 | 沙箱内网络访问 | 首次连接 | 有 `network.client` 权限，不被沙箱阻断 | ✅ 已实测（16.2 / 18.6） |
| TC-59 | 文件读写权限 | 打开 / 保存 `.sql` | 有 `files.user-selected.read-write`，越权路径被拒绝 | 🖐 |
| TC-60 | 多版本兼容 | 对不同版本 PostgreSQL 跑 `test-postgres-version-matrix.sh` | 用例全通过；失败项记入 `Docs/兼容性矩阵.md` | 📦 |
| TC-61 | 中 / 英文残留 | 切换语言后遍历全部界面 | 无硬编码残留（NFR-I18N-02） | 🖐 + 🤖 |
| TC-62 | 崩溃零容忍 | 完成 AC-REL-04 全流程 | 无崩溃日志（`~/Library/Logs/DiagnosticReports`） | 🖐 |
| TC-63 | Keychain 密码不落盘 | 保存连接后检查容器与配置文件 | 密码只在钥匙串；配置 JSON 无密码字段 | 🖐 |

---

## 4. 真实库验收清单（AC-REL-04）

> 建议在一个专用的 PostgreSQL 测试库上执行；执行前准备好只读账号与一个可写测试库。

| 序号 | 步骤 | 勾选 |
|---|---|---|
| 1 | 新建连接 → 测试连接成功 → 保存 → 重启 App 连接仍在 | ⬜ |
| 2 | 选中连接 → 执行 `SELECT version(), current_database(), current_user;` | ⬜ |
| 3 | 执行多语句（建表 / 插入中文与 NULL / 查询），切换结果集 | ⬜ |
| 4 | 执行 DML 观察影响行数（INSERT / UPDATE / DELETE） | ⬜ |
| 5 | 执行 `SELECT pg_sleep(30);` 并点「停止」→ 观察取消提示 | ⬜ |
| 6 | 对象树：服务器 → 多数据库 → schema → 表 → 列（含跨库） | ⬜ |
| 7 | 结果区导出 CSV / JSON 并用 Excel / 文本编辑器打开检查 | ⬜ |
| 8 | 结果复制粘贴到 Excel（UC-09） | ⬜ |
| 9 | 打开 `.sql` 文件 → 编辑 → 保存 / 另存为；新建页签编号递增 | ⬜ |
| 10 | 编辑菜单六项（查找 / 替换 / 跳转 / 缩进 / 清除 / 格式化） | ⬜ |
| 11 | 补全（F5 / Esc / ⌃Space）与语法检查 | ⬜ |
| 12 | 保存查询 → 重启 → 回填；查询历史回填与清空 | ⬜ |
| 13 | 语言切换中 / 英并重启；删除连接二次确认；记忆上次连接 | ⬜ |
| 14 | 拖拽编辑区 / 结果区分隔条 | ⬜ |
| 15 | 错误路径：错误密码 / 错误端口 / 不存在表 / 不合法 SQL（AC-REL-05） | ⬜ |
| 16 | 中文输入法：组字 → 回车提交 → 继续输入英文，光标不跳到串首（TC-64） | ⬜ |
| 17 | 连接显示名形如 `PGConn (postgres)`（TC-65）；载入已保存查询 / 切换页签内容不串（TC-66） | ⬜ |
| 18 | 对象树右键菜单：连接 / 断开 / 编辑连接；有权限账号出现「新建数据库…」、无权限账号不出现（TC-67 ~ TC-70） | ⬜ |
| 19 | 全流程结束后检查无崩溃报告（AC-REL-06） | ⬜ |

**验收记录（执行后填写）**

| 日期 | 版本 | 环境 | 结果 | 执行人 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

---

## 5. 缺陷回归记录（模板）

| 编号 | 现象 | 根因 | 修复 | 回归用例 | 状态 |
|---|---|---|---|---|---|
| BUG-001 | 对象树展开 `public` 后没有下一层 | 旧 UI 用 `List` + 嵌套 `DisclosureGroup`，第 3 层起不可靠 | 改为扁平化树（T-03） | TC-41 / TC-42 | ✅ 已修复（待真实库确认） |
| BUG-002 | 页签编号在关闭页签后会重复 | 用 `tabs.count + 1` 生成标题 | 引入 `TabNumberGenerator`（T-16） | TC-38 | ✅ 已修复 |
| BUG-003 | 格式化 SQL 会丢标识符 | 分词器跳过裸标识符后未回填原文 | 保留 gap 原文（T-19） | TC-36 | ✅ 已修复 |
| BUG-004 | 「替换」按钮提示无此方法 | `NSFindPanelAction` 无 `showReplacePanel` | 改用 `NSTextFinder.Action.showReplaceInterface` | TC-36 | ✅ 已修复 |
| BUG-005 | 中文输入法：打几个字回车提交后，继续输入英文字符会插到字符串开头 | ① 组字期间 `textDidChange` 就把未提交文本写入绑定；② SwiftUI 回写时用 `textView.string = text` 整段替换，重置 `NSTextInputContext` 与撤销栈；③ 提交回调栈里同步重设全量文本属性 | 新增 `Core/EditorTextSync.swift` 判定外部改动；组字期间绝不回写；外部改动改用 `textStorage.replaceCharacters` + `didChangeText`；高亮延后到下一个 runloop（T-22） | TC-64 / TC-66 | ✅ 已修复（待输入法人工回归） |

---

## 6. 变更记录

| 版本 | 日期 | 变更摘要 |
|---|---|---|
| v1.0 | 2026-09-21 | 首版：自动化基线（80 单测 / 25 链路 / 版本矩阵脚本）、56 条功能用例、9 条非功能用例、AC-REL-04 验收清单、缺陷回归模板 |
| v1.1 | 2026-09-21 | 新增 TC-64（中文输入法提交后光标不跳，BUG-005 回归）、TC-65（连接显示名含登录用户名，FR-CONN-14）、TC-66（外部改动不被编辑器吞掉）；缺陷表新增 BUG-005；验收清单补 2 条 |
| v1.2 | 2026-09-21 | 新增 TC-67 ~ TC-70（对象树服务器节点右键菜单：连接 / 断开 / 编辑连接、建库入口按权限呈现、新建数据库并入树、断开与重连语义，FR-META-11）；自动化基线更新为 89 单测 / 28 链路；验收清单补 1 条 |
