# 接管记录 v2.3（2026-09-21 夜间）

> 背景：原 worker（会话 `<会话 id>`，cwd `~`）在第 47 轮完成后休眠。
> 用户夜间从手机上下达指令，由**接替会话**继续推进，并明确要求：
> **不执行任何编译与测试**（编译 / 打包会触发钥匙串访问），且不需要中途确认。

## 1. 本轮做了什么

### 1.1 需求侧（SRS 升 v2.3）

| 项 | 内容 |
|---|---|
| 调研 | 对标 DBeaver / Navicat / DataGrip / TablePlus / Postico 2 / pgAdmin 4 / Beekeeper / HeidiSQL / Sequel Ace / VS Code MSSQL，40+ 官方来源；结论落在 SRS `10.8 主流客户端基础功能清单`（含「本工程处置」列） |
| 新增需求 | **44 条**：FR-CONN-15 ~ 20、FR-EXEC-14 ~ 17、FR-EDIT-25 ~ 27、FR-META-12 ~ 14、FR-RES-08 ~ 13（FR-RES-07 状态上调为 🟡）；新模块 FR-DATA（5）、FR-DDL（5）、FR-DIAG（4）、FR-SESS（3）、FR-IO（5） |
| 新增风险 | R-18（沙箱下不得调用 `pg_dump` / `ssh` 等外部程序，须先选型）、R-19（内联编辑写回）、R-20（会话操作权限）、R-21（客户端排序 / 筛选只作用于已加载行） |
| 任务 | T-25 ~ T-41；同步更新编号索引、追溯表、变更记录 |

### 1.2 代码侧（Core 逻辑为主，未编译未运行）

| 文件 | 内容 | 对应需求 |
|---|---|---|
| `Core/ResultView.swift`（新） | 客户端排序（多列 / 稳定 / 数字感知 / 本地化自然序 / NULL 位置）、筛选（10 种运算符 / AND 组合 / NULL 语义）、分页（页数、越界夹取、每页档位） | FR-RES-07 ~ 10 |
| `Core/SQLGenerator.swift`（新） | `selectRows` / `countRows` / `selectTemplate` / `insertTemplate` / `createTable` / `dropTable` / `truncateTable` / `explain`（PG 与 GBase 分页语法、schema 限定、标识符引用走方言） | FR-DATA-01 ~ 03、FR-DDL-01 ~ 02、FR-DIAG-01 |
| `Core/ExplainPlan.swift`（新） | `EXPLAIN` 文本与 `FORMAT JSON` 两种解析 → 统一扁平节点模型（层级 / 代价 / 估算行数 / 实际耗时 / loops / 明细行）、摘要（最贵 / 最慢节点、全表扫描计数） | FR-DIAG-01 ~ 02 |
| `Core/SessionMonitor.swift`（新） | 会话列表解析（兼容 `pg_stat_activity` 与 `SHOW PROCESSLIST` 两套列名）、时间戳解析与运行时长、等待 → 活跃 → 空闲排序 | FR-SESS-01 |
| `Core/Dialects.swift`（改） | 协议新增 `serverActivityQuery()` / `cancelSessionStatement(pid:)` / `terminateSessionStatement(pid:)`，并给其它方言返回 nil 的默认实现；PG 用 `pg_stat_activity` + `pg_cancel_backend` / `pg_terminate_backend`，GBase 用 `SHOW PROCESSLIST` + `KILL QUERY` / `KILL`（待实测） | FR-SESS-01 ~ 02 |
| `Core/ResultExporter.swift`（改） | 新增 TSV / Markdown / `INSERT` 三种导出；INSERT 按列类型决定是否加引号、`'` 翻倍、表名列名走方言引用 | FR-RES-11 |
| `Core/Localization.swift`（改） | 新增 3 条导出文案（中英齐备，已校验 164 个 key 无缺失） | NFR-I18N |
| `App/Views/ResultTableView.swift`（改） | 导出菜单新增 TSV / Markdown / INSERT 三项 | FR-RES-11 |
| `App/AppState.swift`（改） | 导出时把页签名与连接方言传给 `ResultExporter` | FR-RES-11 |
| `PostgresClient.xcodeproj` | 重新执行 `xcodegen generate`（新文件已进工程，PostgresNIO 关联已由 `patch-xcodeproj.sh` 修复） | FR-BLD-01 ~ 02 |

### 1.3 测试侧（已编写，**未编译未运行**）

新增 4 个测试文件 + 扩容 1 个，**共 76 项**：

- `Tests/ResultViewTests.swift` 22 项
- `Tests/SQLGeneratorTests.swift` 18 项
- `Tests/ExplainPlanTests.swift` 14 项
- `Tests/SessionMonitorTests.swift` 11 项
- `Tests/ResultExporterTests.swift` 新增 11 项

## 2. 没做什么

- **没有编译、没有跑测试、没有打包**（用户明确禁止：会触发钥匙串访问）。
- 除 `ResultExporter` 的导出菜单外，**新能力都没有接 UI**：排序 / 筛选 / 分页控件、表数据浏览入口、执行计划面板、会话监控窗口、DDL 查看等，见任务 T-25 ~ T-40。
- 未实现 `pg_dump` 封装与 SSH 隧道：**必须先做 T-38 的沙箱选型**（R-18），否则会返工。
- 未改 GBase 驱动层（仍阻塞在无实例）。

## 3. 怎么验证（用户在场时执行）

```bash
cd ~/Projects/PostgresClient

# 1) 文档表格列数校验（本轮改过 7 处表格，已通过一次）
python3 Scripts/check-doc-tables.py

# 2) Core 单测：预期由 89 项 → 165 项（新增 76 项）
./Scripts/verify-core.sh

# 3) 本地端到端链路：回归 28 项，确认导出改动未影响链路
./Scripts/test-local-query-path.sh

# 4) App 打包 + 启动冒烟（会触发钥匙串）
./Scripts/build-app.sh && open dist/PostgresClient.app
```

已知静态自查（无编译器参与）：所有改动的 Swift 文件括号 / 中括号 / 圆括号配平（含字符串插值），本地化 164 个 key 中英齐全，Xcode 工程已包含 8 个新文件。
**这些都不等于「能编译通过」**，首次编译仍可能有类型推断、可见性一类问题，请以第 2 步结果为准。

## 4. 建议的下一步顺序

1. `./Scripts/verify-core.sh` → 修编译 / 断言问题（最优先，先让新增 76 项单测变绿）。
2. T-28 表数据浏览入口（双击表 → 新页签）：用户感知最强、Core 已备好。
3. T-25 结果集排序 / 筛选 / 分页 UI：Core 已备好，接表头与筛选条即可。
4. T-30 会话监控窗口 + T-29 执行计划面板：Core 已备好，属新窗口 / 新面板。
5. T-38 沙箱选型（写 ADR）→ 再决定 FR-IO-04 与 FR-CONN-18 的实现路线。

## 5. 风险与注意

- **并发施工**：原 worker 会话未关闭（进程存活、空闲）。若它被唤醒继续改同一批文件，可能与本轮改动冲突；接续前先确认它没有在跑（`<会话管理目录>/inject/servers.json` 可查存活会话）。
- 本工程**没有 git 仓库**（`git status` 报 not a repository）。本轮改动前的备份放在
  `.backups/20260921-takeover/`（SRS、`Dialects.swift`、`project.pbxproj`）。
- `ResultExporter.insertStatements` 的表名当前取页签标题（未绑定文件时用 `table_name` 占位），导出前需要用户确认，见 T-26 未勾选项。
- GBase 侧的 `SHOW PROCESSLIST` / `KILL QUERY` 属 MySQL 协议通用做法，但**未在真实 GBase 8a 上验证**。
