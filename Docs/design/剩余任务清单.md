# 剩余任务清单（A 组与后续）

> 来源：`python3 Scripts/report-requirements.py`（从 `Docs/需求规范书.md` 机械派生，**不手工维护**）。
> 本文件只做一件事：把「谁来做」分清楚 —— **我做**的（代码）与**需要需求提出者**的（真机 / 图形界面 / 硬件）。
> 生成时间：2026-09-23。整体：FR 165 条（✅108 / 🟡23 / ⬜34）、NFR 43 条（✅29 / 🟡12 / ⬜2）、AC 12 条（✅7 / 🟡1 / ⬜4）。

## 一、我做（代码侧，按优先级）

| # | 条目 | 现状 | 要做的 |
|---|---|---|---|
| 1 | ~~**FR-DDL-03** 表设计器：索引 / 外键 / 约束~~ | ✅ **已完成**（`1a0b9d8`，真机脚本 `Scripts/test-table-index-fk.sh` 退出码 0） | — |
| 2 | ~~**FR-RES-13** 超大结果集流式落盘~~ | ✅ **Core + CLI 已完成并实测**（游标逐页 / 230 MB 文件 / 峰值 44 MB）；**GUI 导出入口仍走一次性取回**，接游标属后续 | — |
| 3 | ~~**NFR-AI-04** 成本与配额~~ | ✅ **已完成**（本轮：会话级账本 + 提前拦 + 6 项单测；边界见 R-41） | — |
| 4 | ~~**NFR-AI-06** 本地模型优先~~ | ✅ **客户端全链路已用本地假端点验证**（`Scripts/test-local-endpoint.sh`：免 Key、零外发、关闭开关零请求）；**真实 Ollama / vLLM 联调仍待环境** | — |
| 5 | ~~**FR-IO-04** 备份 / 恢复真实执行~~ | ✅ **执行层已打通、真机往返成功**（`Scripts/test-backup-restore.sh`）；并新增客户端工具版本前置检查（R-42）。**GUI 入口与沙箱子进程继承（R-18 / D5）仍待你定选型** | — |
| 6 | ~~**FR-SESS-01 / 02** 会话列表 / 取消终止~~ | ✅ **界面已接 + 真机验证**（`Scripts/test-session-management.sh`：真取消一条 `pg_sleep`、权限不足如实报错） | — |
| 7 | ~~**FR-AI-07** 合成数据生成器界面~~ | ✅ **已完成**（自动推断规格 + 面板 + 写入走审批；`Scripts/test-synthetic-data.sh` 退出码 0，同 seed 指纹一致） | — |
| 8 | ~~**FR-RES-07** 超大结果集虚拟滚动~~ | ✅ **已复核**：机制齐（分页控件 + AppKit view-based 行复用 + 流式导出）；**大表实测需人工**（NFR-PERF-02 同一阻塞） | — |
| 9 | ~~**FR-META-15** 分组视图~~ | ✅ **已复核并改状态**：代码早已接线（15 项单测），属文档落后于代码；剩人工切换看一眼 | — |
| 10 | ⬜ 进阶可选（36 条，如 FR-AI-03/04/10/11/13/14/15、FR-CONN-15~18、FR-IO-06、FR-RES-14） | 未开始 | 按需求提出者取舍后再排 |

## 二、需要需求提出者（我做不了的部分，以及为什么）

| 类别 | 条目 | 阻塞原因 |
|---|---|---|
| **图形界面手工验收** | 手工清单 A ~ I（`Docs/手工验收运行手册.md`） | 需要人点一遍：弹窗、菜单响应、快捷键、面板交互。agent 无图形会话 |
| **对象树界面验收** | FR-META-01 / 09 / 10 | 服务层已在真机 PG 18.6 验过；剩下的是"在界面上展开到列、看空节点与失败隔离" |
| **GBase 端到端** | FR-META-02、FR-META-13 的 GBase 分支 | 无 GBase 实例；驱动本身未实现 |
| **多版本兼容** | NFR-COMP-01（macOS 14/15）、NFR-COMP-02（Intel/Rosetta）、NFR-COMP-03（PG 12–17） | 需要对应机器与实例；现有实机为 macOS 27 / arm64 / PG 18.6 |
| **性能基线** | NFR-PERF-01 ~ 05 | 需要图形界面 + 大结果集 + 录屏/采样 |
| **浏览器与终端回归** | FR-EDIT-34 剩余（下载走授权目录）、FR-EDIT-29 | 图形界面交互 |
| **发布形态凭据** | NFR-SEC-01 的发布形态（系统凭据存储） | 需签名 / 公证与真机验收 |
| **外部端点** | FR-AI-02 / 05 的真实模型端到端 | 需要模型端点与 API Key |

## 三、纪律（写在清单里，免得两头都忘）

1. **每条完成都要有可复跑的验证**：单测 → 真机脚本 → 手工清单，三层按需，不含糊。
2. **状态只在验证之后改**：代码写完但没人点过的，标 `🟡（UI 已接，待人工验证）`，不标 ✅。
3. **文档漂移要当场纠正**：代码已实现而文档写「未实现」的（如 FR-DDL-03），先核再改，避免重复实现。
4. **每轮提交都推**：本地提交一律推送到远端。

---

## 二、十个待开发功能（按优先级排序，2026-09-23 梳理）

**筛选口径**：从 36 条 ⬜ 里挑十件 —— 排除「需要外部环境或依赖选型」的（FR-DRV-08/09 驱动需 D3/D4、FR-CONN-18 SSH 需沙箱选型、FR-IO-06/FR-RES-14 需 xlsx 依赖、FR-AI-03/04/10 需模型端点或 MCP 依赖、NFR-COMP-04/05 需实例）。
**排序依据**：① 防事故（写错库 / 拼串注入）> ② 每日高频 > ③ 数据进出 > ④ 诊断与可读性 > ⑤ 战略大件（放最后，因为它最大且前置依赖已备）。

| # | 条目 | 为什么排这个位置 | 成本 | 依赖 | 我能独立验证吗 |
|---|---|---|---|---|---|
| **1** | ~~**FR-EXEC-17 查询参数**~~ ✅ **已完成**（v3.95，`Scripts/test-query-parameters.sh` 退出码 0） | **防事故**：手工拼串是 SQL 注入与"引号地狱"的根源；且天天用 | 中 | 无 | ✅ 单测 28 项 + 真机（恶意值当数据插入、表仍在） |
| **2** | ~~**FR-IO-03 CSV / JSON 导入**~~ ✅ **已完成**（v3.96，`Scripts/test-data-import.sh` 退出码 0；**`COPY FROM STDIN` 未接**，见 R-43） | 数据进出刚需 | 大 | 无 | ✅ 真机逐项核对 |
| **3** | ~~**FR-IO-02 整表 / 整库导出**~~ ✅ **已完成**（v3.97，`Scripts/test-table-export.sh` 退出码 0；**`COPY … TO STDOUT` 受驱动限制未做**，实测证据 + 见 R-44） | 承接 FR-RES-13 的「取」 | 中 | 无 | ✅ 真机 + 回归 |
| **4** | ~~**FR-EDIT-25 命令面板 ⌘K**~~ ✅ **已完成并修好死命令**（v3.98 面板 + v3.105 接线：**8 条命令原本设了标志位却没人读、点了没反应**，连同 1 条设错标志位一并修复；新增**第 9 项门禁** `check-palette-wiring.py` 永久拦住这类缺陷；⌘K 已登记 `AppShortcut`） | 高频效率 | 中 | 无 | ⚠️ 逻辑已单测；界面点击待人工 |
| **5** | ~~**FR-META-12 全库对象搜索**~~ ✅ **已完成（含界面面板）**（v3.99 Core + CLI + 真机；v3.105 交付 `ObjectSearchPanel` 与两个入口，`ObjectSearch.Outcome` 收口为可测纯函数） | 库一多就"找不到那张表" | 中 | 无 | ✅ 真机（本机跨 schema + 217 只读） |
| **6** | ~~**FR-RES-12 复制为多格式**~~ ✅ **已完成**（v3.100，Core 13 项单测 + 右键「复制为…」；真机：复制出的 INSERT 执行回另一张表逐值一致） | 小成本高日常价值 | 小 | 无 | ✅ 单测 + 真机往返 |
| **7** | ~~**FR-CONN-16 连接颜色与环境标签**~~ ✅ **已完成**（v3.101，Core 12 项单测 + **生产标签强制高危确认**的联动 + 脚本验证；界面着色待人工） | **防连错库** | 中 | 无 | ✅ 脚本（持久化/联动）+ 人工看界面 |
| **8** | ~~**FR-DATA-05 单行详情 / JSON 编辑器**~~ ✅ **已完成（含界面侧栏）**（v3.102 Core + CLI；v3.105 交付 `RowDetailPanel` + 结果区开关，翻页/换结果集作废选中以免错行；真 bytea 走 UTF-8 解码与 Core 摘要未本地化分别见需求行与 **R-45**） | 宽表与长 JSON 现在几乎没法看 | 中 | 无 | ✅ 单测 + 真机 |
| **9** | ~~**FR-DIAG-03 慢查询排行**~~ ✅ **Core + CLI 已完成**（v3.103，13 项单测 + 排行 SQL 真机跑通；**真扩展未验证** —— 本机精简构建不带该扩展、217 装扩展是对别人服务器的写操作） | 真实排查能力 | 中 | 实例需装扩展 | ✅ 单测 + 同名视图真机 |
| **10** | ~~**FR-AI-13 查询记忆层 S2 + 补全**~~ ✅ **已完成（Core + CLI + 编辑器联动）**（v3.104，30 项单测 + 产品归档格式脚本验证） | **战略价值最高**且不需要模型端点 | 大 | 无 | ✅ 单测 + 脚本 |

**下一批进行中**：FR-AI-14 ✅（v3.108）→ FR-AI-15 ✅（v3.109）→ FR-CONN-17 ✅（v3.110）→ 下一步 FR-DDL-04 Schema 对比与同步。原候补第 11：FR-CONN-15 连接分组 / FR-IO-05 备份恢复到指定库（`--clean` / `--section` / `--jobs` 与失败续跑）/ FR-SESS-03 服务器级对象管理。

---

## 四、待开发功能清单（全量盘点，2026-09-23）

**口径**（数字由 §10.1 索引派生、有门禁钉住，不是手抄）：需求 205 条 = **FR 165**（111 ✅ / 30 🟡 / 24 ⬜）+ **NFR 40**（27 ✅ / 11 🟡 / 2 ⬜）；另有 12 条 AC 验收项不计入需求数。

### 4.1 未开始（⬜ 26 项 = FR 24 + NFR 2）

「可验证性」这一列是**决定先做谁的关键**：能单测 + 能真机/脚本复现的，排前面；只能靠人手点的，排在后面等一并验收。

| 编号 | 功能 | 为什么值得做 | 规模 | 外部依赖 | 可验证性 |
|---|---|---|---|---|---|
| FR-AI-14 | 参数化与技能候选（同一 SQL 的不同取值归并成带槽模板；跨天频次 / 时刻集中度 / 是否只改参数四条判据） | **建在刚交付的记忆层上**（粗指纹 / 频次 / 连接隔离已就位），是"例行任务建议"的落地 | 中 | 无 | ✅ 纯 Core 统计，可单测 + 脚本 |
| FR-AI-15 | 记忆治理（可浏览来源 / 单条删除 / 整层清空 / 本次执行不记录） | 记忆层不能是黑箱；用户必须能管住"客户端记住了我什么" | 中 | 无 | ✅ 单测 + 脚本 |
| FR-CONN-17 | 只读连接与启动 SQL（连接级拒绝写语句；连接后自动执行 `SET search_path` 等） | **防误写生产库**；安全联动已有基座（ExecutionSafety / 生产标签强制确认） | 中 | 无 | ✅ 单测 + 真机（217 只读） |
| ~~FR-DDL-04~~ ✅ 已完成（v3.114，Core + CLI + 真机脚本；界面差异视图待做） | Schema 对比与同步（两个库 / 两个连接的结构差异 + 生成同步脚本） | DBA 刚需；纯 diff 算法 | 大 | 无 | ✅ 单测 + 两个本地集群对拍 |
| ~~FR-IO-05~~ ✅ 已完成（v3.115，Core + CLI + 真机脚本；界面恢复入口待做） | 备份恢复到指定库（`--clean` / `--section` / `--jobs` 与失败续跑） | 已有 `pg_dump` / `pg_restore` 基础设施与版本守卫，接着做最省力 | 中 | 本地实例 | ✅ 真机往返 |
| FR-DATA-04 | 结果集内联编辑（改单元格 / 加行 / 删行，提交前预览将执行的 DML） | 高频；DML 生成是纯函数，安全闸门（Safe Mode）已有 | 大 | 无 | ✅ 单测 + 真机（本地集群写） |
| FR-CONN-19 | 连接 URL 导入 / 导出（`postgres://…` 一行建连；导出配置包） | 小投入、立刻可用；纯解析与序列化 | 小 | 无 | ✅ 单测（可测性最高） |
| FR-DIAG-04 | 数据库统计面板（表大小 / 索引命中 / 连接数 / 缓存命中） | 补齐诊断能力；本机与 217 都能只读跑 | 中 | 无 | ✅ 真机（只读） |
| FR-DATA-06 | 外键引用导航（由单元格跳到被引用表并带入条件） | 复用已成实现的 FK 元数据；查数据动线更短 | 中 | 无 | ✅ 单测 + 真机 |
| FR-EDIT-26 | 主题与字体（跟随系统 / 手动指定；等宽字体与字号可调） | 用户可感的日常体验 | 小 | 无 | ⚠️ 逻辑可单测，观感需人工 |
| ~~FR-CONN-15~~ ✅ 已完成（v3.113，Core + CLI + 侧边栏分组；折叠点击待人工） | 连接分组 / 文件夹（按项目 / 环境组织大量连接） | 连接一多就乱；纯 UI + 持久化 | 小 | 无 | ⚠️ 持久化可测，界面需人工 |
| FR-CONN-20 | 连接保活心跳（空闲按间隔发轻量查询） | 防中间件掐长连接 | 小 | 无 | ✅ 单测（可注入时钟） |
| ~~FR-AI-11~~ ✅ 已完成（v3.112，Core + CLI + 脚本；界面历史入口待做） | specs 版本化与重跑（保存 / 版本化 / diff / 回滚 / 重跑） | 数据任务的工程化闭环 | 中 | 无 | ✅ 单测 + 脚本 |
| FR-EDIT-27 | 多光标与列编辑（⌥ 拖拽 / ⌘D） | 编辑器进阶效率 | 中 | 无 | ❌ 只能人手点 |
| FR-DDL-05 | ER 图 / 关系图（由外键生成可视化） | 理解库结构最快的方式 | 大 | 无 | ❌ 只能人工看图 |
| FR-SESS-03 | 服务器级对象管理（角色 / 表空间 / 扩展的浏览与增删改） | DBA 日常；本地集群能测 | 大 | 本地实例可写 | ⚠️ 逻辑可测，界面需人工 |
| FR-CONN-18 | SSH 隧道（跳板连接不可直连的库，密码 / 私钥两种） | 生产库常见前置 | 大 | **需要 SSH 跳板环境** | ⚠️ 需环境 |
| FR-DRV-08 | GBase 8a 驱动实现 | 国产库适配 | 大 | **需要真实 GBase 实例** | ⚠️ 需环境 |
| FR-DRV-09 | MySQL 驱动实现（`DatabaseType` 新增 `mysql` 档） | 覆盖面扩大 | 大 | **需要真实 MySQL 实例** | ⚠️ 需环境 |
| FR-RES-14 | 结果集导出 Excel（.xlsx） | 交付常见需求 | 中 | **需第三方库选型拍板** | ✅ 可测，但先要决策 |
| FR-IO-06 | Excel（.xlsx）导入映射向导 | 同上 | 中 | **需第三方库选型拍板** | ✅ 可测，但先要决策 |
| FR-AI-03 | 对话式诊断（解读执行计划 / 慢查询 / 锁阻塞） | 智能体差异化 | 大 | **需模型端点** | ⚠️ 需端点 |
| FR-AI-04 | DBA 维护任务编排（自然语言生成并执行维护任务） | 同上 | 大 | **需模型端点** | ⚠️ 需端点 |
| FR-AI-10 | 互操作接口 MCP 双向（作为 host 接入 / 作为 server 暴露能力） | 生态位 | 大 | **需 MCP 端与协议决策** | ⚠️ 需环境 |
| NFR-COMP-04 | 支持 GBase 最新版本并向下兼容至 8a | 兼容性承诺 | 中 | **需要 GBase 实例** | ⚠️ 需环境 |
| NFR-COMP-05 | 支持 MySQL 最新大版本并向下兼容最近 5 个大版本 | 兼容性承诺 | 中 | **需要 MySQL 实例** | ⚠️ 需环境 |

### 4.2 部分完成（🟡 41 项）—— 按"谁在挡"分四类

**① 等人手确认（16 项）**：`FR-AI-07`、`FR-CONN-16`、`FR-DATA-02`、`FR-DDL-03`、`FR-EDIT-25`、`FR-EXEC-15`、`FR-EXEC-17`、`FR-META-13`、`FR-META-15`、`FR-RES-07`、`FR-RES-08`、`FR-RES-09`、`FR-RES-10`、`FR-RES-12`、`FR-SESS-01`、`FR-SESS-02`
—— 代码与脚本都过了，剩下的是**只有人手能确认**的部分（界面点击、着色、呈现是否符合预期）。`FR-RES-07` 的尾巴还挂着 NFR-PERF-02 的大表实测。

**② 我自己的活（5 项）**：
- `FR-META-12`（全库对象搜索**界面面板**）、`FR-DATA-05`（行详情**侧栏**）—— **正在做**（本轮）。
- `FR-IO-03`（`COPY FROM STDIN` 尚未接线）—— **能做**：vendor 的驱动支持 `copyFrom(table:columns:format:)`，只是当初没接；接线后大批量导入会快一个量级。
- `FR-IO-04`（GUI 入口与沙箱路线**待定**）—— **需要拍板**：走沙箱内的用户选目录，还是放开临时例外。
- `NFR-AI-04`（真实请求路径已接，真端点联调待做）—— 与你搁置的 DeepSeek 实测同一件事。

**③ 受环境 / 驱动限制（4 项）**：`FR-DIAG-03`（真 `pg_stat_statements` 未验证：本机精简构建不带该扩展，217 上装扩展是对别人服务器的写操作）、`FR-IO-02`（`COPY … TO STDOUT` 受驱动限制：vendor 驱动不支持 CopyOut，见 R-44）、`FR-RES-13`（GUI 导出仍一次性取回）、`NFR-AI-06`（真实 Ollama / vLLM 待环境）。

**④ 尾巴没写清（16 项）**：`FR-AI-12`、`FR-EDIT-29`、`FR-EDIT-34`、`FR-META-01`、`FR-META-02`、`FR-META-09`、`FR-META-10`、`NFR-COMP-01`、`NFR-COMP-02`、`NFR-COMP-03`、`NFR-PERF-01`、`NFR-PERF-02`、`NFR-PERF-03`、`NFR-PERF-04`、`NFR-PERF-05`、`NFR-SEC-08`
—— 这几条**只写了 🟡 或"待验证"，没写差在哪**。这是**我自己的文档债**：🟡 不写清尾巴，下一个人（包括我自己）就得重新推一遍。做下一批时要先把它们补成"差什么、谁来补、怎么验"。

### 4.3 建议下一批十项（第 11 ~ 20，按优先级）

排序依据：① 战略价值（AI 记忆层延伸优先）② **可验证性**（能单测 + 真机/脚本复现优先）③ 无外部依赖 ④ 复用已有基座。

| 序 | 编号 | 功能 | 为什么排这里 |
|---|---|---|---|
| 11 | ~~FR-AI-14~~ ✅ 已完成（v3.108，Core + CLI + 脚本；界面入口待做） | 参数化与技能候选 | 直接建在刚交付的记忆层上（粗指纹 / 频次 / 隔离都现成），纯统计、不需要模型端点 |
| 12 | ~~FR-AI-15~~ ✅ 已完成（v3.109，Core + CLI + 脚本；通用层存储与「本次执行不记录」界面接线待做） | 记忆治理 | 记忆层交付后必须马上可检视 / 可删除，否则是黑箱；纯本地可测 |
| 13 | ~~FR-CONN-17~~ ✅ 已完成（v3.110，Core + CLI + 界面字段；界面点击待人工） | 只读连接与启动 SQL | 防误写生产库，安全收益最高；拦截逻辑有现成基座 |
| 14 | FR-DDL-04 | Schema 对比与同步 | 纯 diff、可单测、可用两个本地集群真机对拍；DBA 刚需 |
| 15 | FR-IO-05 | 备份恢复到指定库 | 复用已有备份基础设施与版本守卫，能真机往返 |
| 16 | ~~FR-DATA-04~~ ✅ 已完成（v3.111，Core + CLI + 真机脚本；表格内编辑交互待做） | 结果集内联编辑（先预览 DML） | 高频功能；DML 生成可单测，安全闸门已有 |
| 17 | FR-CONN-19 | 连接 URL 导入 / 导出 | 小投入高可测性，立刻可用 |
| 18 | FR-DIAG-04 | 数据库统计面板 | 只读可真机跑（本机 + 217），补齐诊断 |
| 19 | FR-DATA-06 | 外键引用导航 | 复用已有 FK 元数据 |
| 20 | FR-EDIT-26 | 主题与字体 | 用户可感、纯本地（观感需人工确认） |

**不进下一批**：需要外部环境的（FR-CONN-18 / FR-DRV-08 / FR-DRV-09 / NFR-COMP-04 / NFR-COMP-05 / FR-AI-03 / FR-AI-04 / FR-AI-10）、需要你先拍板的（FR-RES-14 / FR-IO-06 的 xlsx 第三方库选型、FR-IO-04 的沙箱路线）、以及只能靠人手看图验的（FR-EDIT-27 / FR-DDL-05）。
