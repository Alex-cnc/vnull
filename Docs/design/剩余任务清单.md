# 剩余任务清单（谁做什么）

> 数字与清单**由脚本从 `Docs/需求规范书.md` 机械派生**（状态列是唯一事实来源，本工程禁止手抄状态）：
> `python3 Scripts/report-requirements.py`（`--open` 只看 ⬜ 与 🟡）。
> 本文件只回答两件事：**谁来做**、**按什么顺序**。
> 生成时间：**2026-09-24**（上一版 2026-09-23 已过时，更正明细见文末）。

## 一、还剩多少（2026-09-24 派生）

| 层 | 总数 | ✅ 已实现 | 🟡 部分 / 待验 | ⬜ 未开始 |
|---|---|---|---|---|
| FR | 165 | 111 | 43 | 11 |
| NFR | 43 | 29 | 12 | 2 |
| AC（验收项，不计入需求数） | 12 | 7 | 1 | 4 |

复现命令：`python3 Scripts/report-requirements.py`。数字对不上就是文档漂移，以脚本输出为准。

## 二、我做（代码侧，按优先级）

### 2.1 只差界面入口的 11 项 —— Core / CLI 已交付，**本轮 11 / 11 全部接线完成**（2026-09-24）

判据：需求的「仍未做」里只剩**界面**（接线、面板、入口），没有算法或协议缺口；做完能靠单测 + 门禁 + 现有真机脚本验收。

| 编号 | 需求 | 缺什么 |
|---|---|---|
| ~~FR-CONN-19~~ ✅ 已完成（2026-09-24，v3.125：表单顶部「从连接 URL 导入」；合并规则抽进 `ConnectionURL.FormMerge` + 4 项单测） | 连接 URL 导入 / 导出 | — |
| ~~FR-CONN-20~~ ✅ 已完成（2026-09-24，v3.125：`ConnectionSettingsSheet` 设置面板 + ⌘K `connectionSettings`；下限收进 `KeepAlivePolicy.minimumIntervalSeconds`） | 连接保活心跳 | — |
| ~~FR-DATA-04~~ ✅ 已完成（2026-09-24，v3.131：`InlineEditDraft` 整行快照 + 双击就地编辑 + 预览弹窗 + 13 项单测） | 结果集内联编辑 | — |
| ~~FR-DATA-06~~ ✅ 已完成（2026-09-24，v3.128：右键「跳到被引用行…」+ 多目标选择面板 + `QueryTab.sourceTable`） | 外键引用导航 | — |
| ~~FR-DDL-04~~ ✅ 已完成（2026-09-24，v3.127：`SchemaDiffPanel` + `AppState.schemaDiffPlan`；残留：列级描述英文界面下仍是中文） | Schema 对比与同步 | — |
| ~~FR-DIAG-04~~ ✅ 已完成（2026-09-24，v3.126：`DatabaseStatsPanel` + Core `DatabaseStats.report` +3 项单测） | 数据库统计 | — |
| ~~FR-IO-03~~ ✅ 已完成（2026-09-24，v3.133：**整套导入界面** `ImportPanel`；原判「不是接线而是要做一整套界面」成立，确实做了整套） | CSV / JSON 导入 | — |
| ~~FR-IO-05~~ ✅ 已完成（2026-09-24，v3.131：`BackupRestoreSheet` 扩展出目标库 / 分段 / `--clean` / `--jobs` / 续跑建议） | 备份恢复到指定库 | — |
| ~~FR-AI-11~~ ✅ 已完成（2026-09-24，v3.133：`DataTaskVersionSheet` 历史 / 字段级 diff / 回滚，回滚以新版本留痕） | specs 版本化与重跑 | — |
| ~~FR-AI-15~~ ✅ 已完成（2026-09-24，v3.133：`RoutineCandidatesPanel` 内新增治理页签 + 「本次执行不记录」真生效） | 记忆治理 | — |
| ~~FR-RES-13~~ ✅ 已完成（2026-09-24，v3.131：`exportResult` 按行数分流走游标逐页取 + 逐页写盘） | 超大结果集流式落盘 | — |

### 2.2 本轮已清（原「还没开始、我能独立验证的」两项）

| 编号 | 功能 | 为什么值得做 | 规模 | 可验证性 |
|---|---|---|---|---|
| ~~FR-SESS-03~~ ✅ 已完成（2026-09-24，v3.129 / v3.130：Core + CLI + 界面 + 45 项单测 + 真机脚本 49 断言全过；残留：界面点击待人工、GBase 侧全为离线推断） | 服务器级对象管理（角色 / 表空间 / 扩展的浏览与增删改） | DBA 日常；本机临时集群可写，能真机往返 | 大 | ✅ 单测 + 真机脚本 |
| FR-EDIT-26 | 主题与字体（跟随系统 / 手动指定；等宽字体与字号可调） | 用户可感的日常体验 | 小 | ⚠️ 逻辑可单测，观感需人工 |

### 2.3 我只能把代码写好、验收必须靠人眼的

`FR-EDIT-27`（多光标与列编辑）、`FR-DDL-05`（ER 图 / 关系图）—— 只能人手点或看图，排在人工验收批次里一起去过，不单独立项做。

## 三、需要需求提出者

### 3.1 要环境 / 实例（给了实例，我立刻能出证据）

| 要什么 | 卡住的需求 | 现状 |
|---|---|---|
| 可用 **GBase 8a** 实例 + 版本清单 | FR-DRV-08、FR-META-02、NFR-COMP-04 | 方言层已就绪；无实例，驱动实现与多版本矩阵都动不了 |
| **MySQL** 实例（覆盖窗口内多个版本） | FR-DRV-09、NFR-COMP-05 | 同上；与 GBase 同属 MySQL 协议系，可一并接 |
| **PostgreSQL 12–15 / 17** 实例 | NFR-COMP-03 | 本机 16.2 + 真机 18.6 已实测；矩阵脚本 `Scripts/test-postgres-version-matrix.sh` 已就绪，缺实例 |
| **SSH 跳板**环境 | FR-CONN-18 | 隧道逻辑可写，但没有跳板就验不了"连不上的库真的连上了" |
| **真实模型端点**（Ollama / vLLM / 任何 OpenAI 兼容） | NFR-AI-04、FR-AI-12、FR-AI-03、FR-AI-04 | 入口已备：`./Scripts/test-real-endpoint.sh --endpoint <URL> --model <名>`；本机端口 5000 是 AirPlay，不是模型服务 |
| **MCP 端与协议口径** | FR-AI-10 | 双向互操作需要先定协议边界 |

### 3.2 要拍板（一句话决定就能继续，不必你动手）

| 决策 | 卡住的需求 | 我的建议口径 |
|---|---|---|
| xlsx 第三方库选型（体积 / 许可 / 是否 vendor） | FR-RES-14、FR-IO-06 | 倾向 vendor 进 `Vendor/`，避免运行时下载；选型定了核心逻辑与映射向导我都能做 |
| 沙箱下子进程能否继承父沙箱（R-18 / D5） | FR-IO-04 的「临时例外」路线、FR-CONN-18 的实现方式 | 现有口径是**不放宽沙箱**：沙箱内选目录 + 起不了子进程时给非沙箱构建指引；要改「临时例外」需你点头 |
| FR-AI-12「不得超越当前连接权限」是否加客户端校验 | FR-AI-12 | 现在是同连接 + 服务端拒绝，够用但不显式；加客户端校验是防御性冗余 |
| NFR-COMP-03 的 PG 版本是否接进能力门控 | NFR-COMP-03 | 版本清单拿到后再定，避免把"没验过的版本"写进承诺 |

### 3.3 只能人工验收（代码与脚本都过了，等人点一遍）

`FR-CONN-15`（分组折叠点击）、`FR-CONN-16`（着色与徽标一致性）、`FR-CONN-17`（只读开关点击保存）、`FR-EXEC-15`（清单 F，含"删表后回滚"）、`FR-EXEC-17`（参数面板交互）、`FR-EDIT-25`（⌘K 面板点击）、`FR-EDIT-29`（终端交互）、`FR-EDIT-34`（浏览器下载与拦截）、`FR-RES-07`/`FR-RES-08`（表头与滚动，挂 NFR-PERF-02 大表实测）、`FR-RES-09`/`FR-RES-10`/`FR-RES-12`（筛选 / 分页 / 复制贴进 Numbers）、`FR-META-01`/`FR-META-09`/`FR-META-10`（对象树展开、空态、失败隔离，清单 C）、`FR-META-13`（DDL 页签，清单 G）、`FR-META-15`（分组视图切换）、`FR-DATA-02`（浏览面板，清单 I）、`FR-DATA-05`（行详情侧栏）、`FR-DDL-03`（表设计器面板，清单 H）、`FR-DIAG-03`（真扩展：本机不带、217 不擅自装）、`FR-IO-02`（`COPY … TO STDOUT` 受驱动限制，见 R-44）、`FR-IO-04`（备份 / 恢复面板点击）、`FR-AI-07`（合成数据面板）、`FR-AI-14`（候选面板）、`NFR-I18N-01`（系统控件文案随系统语言）、`NFR-PERF-01 ~ 05`（需图形会话 + 大表 + 采样）。

**2026-09-24 新增待人工点击的界面**：FR-CONN-19（连接表单「从连接 URL 导入」）、FR-CONN-20（「连接设置」保活面板）、FR-DIAG-04（数据库统计面板）、FR-DDL-04（Schema 对比面板）、FR-DATA-06（结果表右键「跳到被引用行…」）、FR-DATA-04（结果集内联编辑：双击改值 / 右键删行 / 预览弹窗）、FR-RES-13（大结果集流式导出与页签日志）、FR-IO-05（备份 / 恢复面板里的「恢复到指定库」）、FR-AI-11（数据任务「版本历史…」）、FR-AI-15（记忆治理页签）、FR-IO-03（「导入数据…」整套面板）、FR-SESS-03（服务器级对象面板）。

手工步骤与检查点见 `Docs/手工验收运行手册.md`。

## 四、纪律（写在清单里，免得两头都忘）

1. **每条完成都要有可复跑的验证**：单测 → 真机脚本 → 手工清单，三层按需，不含糊。
2. **状态只在验证之后改**：代码写完但没人点过的，标 `🟡（UI 已接，待人工验证）`，不标 ✅。
3. **文档漂移要当场纠正**：代码已实现而文档写「未实现」的，先核再改，避免重复实现。
4. **每轮提交都推**：本地提交一律推送到远端。

## 五、本次重写更正了什么（2026-09-24）

1. **数字整体过期**：上一版停在 **FR 165（108 ✅ / 23 🟡 / 34 ⬜）**、并把 NFR 写成 40 条；实际是 **FR 165（111 / 42 / 12）**、**NFR 43（29 / 12 / 2）**。根因是上一版是手抄的 —— 所以本次把「数字」这一段明确标注为**由 `Scripts/report-requirements.py` 派生**，不再手写。
2. **4.1「未开始 26 项」里的 12 项早已交付**，却只在自己那一行划了删除线、表格口径没跟着改，读起来像还没做：`FR-AI-14`、`FR-AI-15`、`FR-CONN-17`、`FR-CONN-19`、`FR-CONN-20`、`FR-DATA-04`、`FR-DATA-06`、`FR-DDL-04`、`FR-IO-05`、`FR-DIAG-04`、`FR-AI-11`、`FR-CONN-15`。未开始实际是 **FR 12 项 + NFR 2 项**，逐条列在 §2.2 与 §3.1。
3. **4.2 ④「尾巴没写清（16 项）」整节过期**：这 16 条（`FR-AI-12`、`FR-EDIT-29`、`FR-EDIT-34`、`FR-META-01/02/09/10`、`NFR-COMP-01/02/03`、`NFR-PERF-01~05`、`NFR-SEC-08`）已经在需求规范书里补成「差什么 / 谁来补 / 怎么验」，本节删去。
4. **4.2 ② 说 `FR-META-12` / `FR-DATA-05`「正在做」**：两者均已交付（含界面面板与侧栏），本节删去。
5. **编号缺陷**：上一版有两个 `## 二`（第 22 行与第 44 行）。本次重排为 一 ~ 五，并把「我做 / 需要需求提出者 / 纪律」三段的顺序理顺。
6. **FR-IO-04 的 GUI 入口已交付**（`BackupRestoreSheet` + `runBackupPlan`，v3.123），上一版把它并进"待定"，读起来像没做；实际只剩**沙箱路线**待拍板（见 §3.2）。
