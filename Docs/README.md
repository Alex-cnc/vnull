# 文档（Docs）

本目录分两类：**三书（跨平台共享事实源）** 与 **产品文档**。其余工程内部文档
（调研、测试用例、兼容性矩阵、发布方案、评审与过程记录）保存在开发者本地，不随仓库发布。

## 三书：macOS 与 Linux 的共享事实源

macOS 版（本仓库）与 Linux 版（公司电脑）是**同一个产品的两个平台实现**，功能必须一致，
只有平台与编程语言不同。所以这三份文档**必须入库**，用 GitHub 同步版本 —— 任何一侧改了
契约，另一侧跟着改。

| 文档 | 定位 | 变更规则 |
|---|---|---|
| [需求规范书.md](需求规范书.md) | **做什么**：需求条目、验收标准、优先级、风险 | 契约（`【契约】`）改动需两侧同时确认；`【macOS】` 段只是实现记录 |
| [产品能力规划说明书.md](产品能力规划说明书.md) | **为什么做、做到什么程度**：能力地图、分期、取舍 | 能力范围变化时更新，两侧共用 |
| [概要设计.md](概要设计.md) | **怎么做**：分层、平台适配层契约、不变量、ADR | §3 是两侧都必须遵守的接口契约；§5 实现级 ADR 各平台独立 |

配套文件：

| 文件 | 说明 |
|---|---|
| [平台实现状态.json](平台实现状态.json) | Linux 侧实现状态的**唯一**登记处（本仓库基线为 macOS） |

三书里的术语约定见 `需求规范书.md` §0.8：`【契约】` = 两侧必须一致，
`【macOS】` = 可替换的实现细节。需求书 §3/§4 的「状态」列是 **macOS 基线**，
Linux 的状态只在 `平台实现状态.json` 里记，避免同一件事有两个真相。

### 三书自动校验（与代码同一个验证闭环）

```bash
python3 Scripts/gen-platform-parity.py --check      # §10.10 等价矩阵与 §10.1/§10.9 一致
python3 Scripts/check-platform-neutrality.py        # 需求书里的平台专属词汇不得比基线更差
python3 Scripts/check-doc-tables.py                 # 表格列数与派生计数一致
```

三项都已挂进 `Scripts/verify-all.sh`，改完文档跑它即可。

## 产品文档

| 文档 | 说明 |
|---|---|
| [功能清单（一页纸）.md](功能清单（一页纸）.md) | 一页纸概览：现在能用什么、还差什么、需要拍板什么 |
| [功能清单（管理视图）.md](功能清单（管理视图）.md) | 完整功能清单，按「能用 / 做了一半 / 还没做」分类，非技术表述 |

## 待补

- **用户手册**：安装、连接数据库、执行查询、导出数据、常见问题 —— 发布前补齐
- **发布说明（Changelog）**：对外版本变更记录 —— 首次发布时建立
- **Linux 侧构建与运行说明**：随 Linux 版首个可运行版本一起补
