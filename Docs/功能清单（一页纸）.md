# 智能数据库客户端 · 进度一页纸

**结论**：日常使用功能已经可用（代码完成 + 479 项自动化测试通过）。
AI 等新功能**底层已做好、界面还没接完**；国产数据库支持、数据导入等**还没做**。
**目前尚未达到可交付状态。**

---

## 一、现在能用

- **连数据库**：支持 PostgreSQL；密码存系统钥匙串，配置文件中不留密码
- **写查询、跑查询**：语法高亮、关键字补全、多段一次执行、执行中随时取消
- **看结果**：表格展示，可按列排序、筛选、翻页、复制
- **导出结果**：CSV / JSON / 表格文本 / Markdown / 可直接回放的插入语句
- **浏览库内对象**：服务器 → 数据库 → 表 → 字段 逐层展开；可按类型分组（表 / 视图 / 序列 / 函数）
- **管理数据库**：新建、修改属性（归属人 / 连接数 / 参数）、删除（必须手输库名才能确认）
- **权限管理**：查看某账号有哪些权限；授权 / 收回前先看到将执行的语句
- **锁与阻塞排查**：谁在等谁、等了多久、卡在哪个对象，可定位到阻塞方
- **备份命令生成**：生成整台服务器的备份命令（只生成，不自动执行）
- **AI 中文转查询**：用中文描述需求自动生成查询语句；结果只进编辑框，**绝不自动执行**；关掉总开关后完全不联网
- **高危语句保护**：不带条件的批量删除 / 更新、删表、清空表等，执行前先弹窗说明风险再确认（开关在工具栏「防护」里，可自行关闭）
- **只跑想跑的那段**：可只执行光标所在的一条语句，或只执行选中的片段，不必每次跑整篇脚本（工具栏「运行范围」切换）
- **键盘快捷键**：工具栏与菜单每个按钮都有快捷键（执行 ⌘↩、停止 ⌘.、查找 ⌘F…），点工具栏「?」可看完整快捷键表
- **右键就能干活**：表 / 视图上双击＝看数据，右键＝浏览数据、生成查询或插入模板、复制表名、查看建表语句；删表 / 清空也只生成语句给你看，不会手一抖就执行
- **大结果集导出**：边读边写文件，不占内存，中途取消不留半截文件
- 中英文界面即时切换

---

## 二、待完成

**① 底层已做好、只差界面（接线即可，见效最快）**
AI 数据任务 / 定时调度 / 导出到指定文件夹 / 审批与审计 / 安全防护；
事务提交回滚按钮；执行计划查看；表数据直接浏览；表结构可视化编辑；角色修改与删除；会话管理界面

**② 还没做的重要项**
SSH 跳板连接｜表格里直接改数据｜导入 CSV / Excel｜
备份与恢复真正执行｜对象搜索｜查看建表语句｜多窗口

**③ 卡在决策上**
备份恢复的真正执行、SSH 跳板连接 —— 取决于第四节第 1 项怎么定

**④ 远景目标（优先级低，不阻塞本期交付）**
国产数据库 GBase 8a 支持（需真实实例与驱动，2026-09-22 经确认从"基线必备"下调）

---

## 三、需要拍板（5 件，不定就做不下去）

1. **软件怎么分发**：上架应用商店，还是自己签名发给客户？（决定备份 / 恢复、SSH 跳板能否真正执行）
2. 要不要 **Excel 导入导出**（需引入第三方组件，涉及许可证）
3. 要不要 **多窗口**
4. **AI 用哪个模型服务**（本地部署 / 云端）+ 密钥（本地部署可做到数据不出内网）
5. **GBase 8a 的版本号 + 测试环境**（没有环境就无法开发与验证）

---

## 四、交付前必须补的三件事

1. **用真实数据库逐项验收** —— 15 步验收清单已备好，目前**一步未跑**
2. **安装包签名与公证** —— 否则客户机器上会被系统拦截
3. **一轮完整回归** —— 把"现在能用"里的功能在真实环境全部点一遍并留记录

---

**建议下一步**：先做第 ① 类（底层已就绪，接线即可，投入小、见效快）；
第 ② 类里建议按日常使用频率排序：先补「表格里直接改数据」与「导入 CSV」，再补 SSH 跳板。

<sub>附进度数字（供参考，由《需求规范书》§10.1 索引表机械派生）：需求共 223 条，已完成 149 ｜ 完成一半 62 ｜ 未开始 12（只算功能需求 168 条则是 113 ｜ 49 ｜ 6）；其中最高优先级的"基线必备" 42 条：已完成 39 ｜ 一半 3 ｜ 未开始 0。更新于 2026-09-25。</sub>
