# -*- coding: utf-8 -*-
"""一次性补丁：把两个决定与 R-29~R-34 的修复结果落进需求规范书。跑完即删。"""
import pathlib
import re

p = pathlib.Path("Docs/需求规范书.md")
t = p.read_text(encoding="utf-8")


def once(old: str, new: str, label: str) -> None:
    global t
    assert t.count(old) == 1, f"{label}: 命中 {t.count(old)} 次"
    t = t.replace(old, new, 1)
    print(f"✅ {label}")


# ---------- 1) 标题条数（实测原本就漂了：44 / 8） ----------
once("### 3.2 SQL 编辑与执行（44 条）", "### 3.2 SQL 编辑与执行（51 条）", "§3.2 标题 44→51")
once("### 3.10 驱动与方言兼容（8 条）", "### 3.10 驱动与方言兼容（9 条）", "§3.10 标题 8→9")

# ---------- 2) FR-EDIT-34 内嵌浏览器 ----------
FR_BROWSER = "| FR-EDIT-34 | **内嵌浏览器页签（编辑器区，完整浏览器）**：在**编辑器区**新增可关闭的浏览器页签（与 SQL 页签同级，**不是**下方面板里的一个页签）；按需求提出者 2026-09-23 拍定做**完整浏览器**而非只读预览 | 常用增强 | 中 | ⬜ | **前置依赖：NFR-SEC-08 统一外发日志先落地** —— 浏览器是本产品第一个「通用出网」入口，没有外发日志就不该上线。① 默认打开空白页 + 地址栏，**不加载任何远程内容**，一切导航由用户显式发起；② 每条出网请求（文档 / 子资源 / 下载）都进同一份外发日志，可按页签与会话过滤（NFR-SEC-08）；③ **独立数据存储**：不与 SQL 客户端的连接信息、凭据共用 Cookie 与缓存目录；④ 下载与本地文件访问沿用**授权目录**模型（`SecureDirectoryAccess`），不静默写盘；⑤ 页签状态（URL / 标题 / 前进后退）随会话持久化，可关闭可恢复；⑥ 平台差异登记于 **§10.9 P-14**（渲染引擎 / Cookie 存储 / 权限提示允许不同，行为契约必须一致） |"
once(
    "\n\n### 3.3 结果集与导出（14 条）",
    "\n" + FR_BROWSER + "\n\n### 3.3 结果集与导出（14 条）",
    "FR-EDIT-34 内嵌浏览器",
)

# ---------- 3) NFR-SEC-08 统一外发日志 ----------
NFR_EGRESS = "| NFR-SEC-08 | **统一外发日志**：所有离开本机的网络请求（智能体模型调用、内嵌浏览器导航与子资源、外部程序调用、更新检查）写入**同一份**可查看 / 可导出 / 可清空的日志；每条含时间 / 目标 / 触发来源（哪个功能、哪个页签或会话）/ 结果 | 基线必备 | ⬜ | ① 关闭智能体且未打开浏览器时日志为空 ——「默认零外发」由此**可验证**，而不再只是一句口号；② 用假端点分别触发智能体 / 浏览器 / 外部程序三类请求，日志条数与类别一一对应；③ 日志**不含密钥**，也不含结果集行数据（与 NFR-SEC-05 同纪律）；④ 浏览器与智能体**共用一份**日志，不各自为政；⑤ 导出格式与既有智能体审计日志一致（JSON / CSV），脱敏走同一处。**本次登记（2026-09-23）**：需求提出者拍定「先做统一外发日志，再做内嵌浏览器」 |"
once(
    "| NFR-SEC-07 | 删除连接时同步删除系统凭据存储中的凭据 | 基线必备 | ✅ | ✅ |",
    "| NFR-SEC-07 | 删除连接时同步删除系统凭据存储中的凭据 | 基线必备 | ✅ | ✅ |\n" + NFR_EGRESS,
    "NFR-SEC-08 统一外发日志",
)

# ---------- 4) §10.9 P-14 ----------
once(
    "| P-13 | 配置与数据目录 |",
    "| P-14 | 内嵌浏览器 | 行为契约一致：默认不加载远程内容、导航由用户显式发起、出网全部进统一外发日志、独立数据存储（不共享 SQL 客户端凭据与 Cookie）、下载走授权目录、页面脚本不得触达客户端能力 | 渲染引擎（WKWebView / WebKitGTK 或 QtWebEngine）、Cookie 存储位置、权限提示方式 | FR-EDIT-34、NFR-SEC-08 |\n| P-13 | 配置与数据目录 |",
    "§10.9 P-14 内嵌浏览器",
)

# ---------- 5) §9 风险：标注修复结果 ----------
FIXES = {
    "R-29": "**已修复（2026-09-23）**：`onTermination` 停生产端、每条语句前查一次取消状态、取消收尾不发 `.finished`（改抛 `CancellationError`）；App 侧成功分支改为 `succeeded: !Task.isCancelled` 且取消同样留痕。`Tests/ExecutionRegistryTests`（6 项）钉住「排队时被取消 → 一条语句都不执行」",
    "R-30": "**已修复（2026-09-23）**：契约改为句柄化（`execute(_:options:handle:)` + `cancel(_:) async -> CancelOutcome`）；Core 新增 `ExecutionRegistry`（只有活跃句柄才发服务端取消，别人的语句一律不碰）；拿不到后端 PID / 取消连接建不起来如实返回 `.failed(reason:)`，界面显示「停止未能下发到服务端…」",
    "R-31": "**部分修复（2026-09-23）**：① 已做 —— `QueryResult.isTruncated` / `truncationLimit`、`QueryLimits.defaultMaxRows = 10_000`（`QueryOptions.default` 从此**带上限**）、`ResultCollector`（到上限多拉一行以区分「正好」与「还有」），结果表头 / 状态栏 / 结果摘要 / 归档四处口径一致（5 项单测）；**仍未做**：② `statementTimeout` 接线与 ③ 按 `fetchSize` 增量 yield（需改事件模型，属两侧共同契约变更）",
    "R-32": "**已修复（2026-09-23）**：`QueryEvent` **删掉 `.affectedRows` 通道**（只剩 `.resultSet(affectedRows:)`，多算一次在类型上已不可能），计数收进 `AffectedRowsTally`；`value` 在一条都没报过时为 `nil`（区分「0 行」与「不报行数」）；5 项单测含「一条 DML 只累加一次」",
    "R-33": "**部分修复（2026-09-23）**：③ 终端改为 `killpg(pid, SIGHUP)` → 1 秒宽限后 `killpg(pid, SIGKILL)`，并在后台队列 `waitpid` 收尸（机制已单独验证：只 `kill(子进程)` 时孙进程存活，`killpg` 后两者都消失）；④ `masterFD` / `childPID` 收进一把锁、写循环整体在临界区；① `connect()` 初始化失败会关掉已建连接再抛；② `disconnect()` 关闭失败留 warning。**仍未做**：`PostgresService` 的 deinit 兜底",
    "R-34": "**已修复（2026-09-23）**：`finish()` 改为「原文件先挪备份 → 移入新文件 → 成功才删备份；失败把原文件放回去」（移动操作可注入，专为测这条失败路径）；`begin()` 打不开句柄时清掉半成品。2 项新测试，其中「注入移动失败后原文件必须完好」正是原实现会丢文件的场景",
}
for rid, note in FIXES.items():
    pattern = re.compile(rf"^(\| {rid} \|.*?)(\s*\|\s*)$", re.M)
    found = pattern.findall(t)
    assert len(found) == 1, f"{rid}: 命中 {len(found)}"
    t = pattern.sub(lambda m: f"{m.group(1)}。{note} |", t, count=1)
    print(f"✅ §9 {rid} 标注修复结果")

# ---------- 6) R-35 浏览器风险 ----------
R35 = "| R-35 | **内嵌浏览器带来新的攻击面与外发面（2026-09-23 拍定为「完整浏览器 + 编辑器区」）**：完整浏览器可加载任意外部内容 —— 这是本产品第一个「通用出网」入口，与「默认零外发、最小外发」的既有纪律直接相撞；同时新引入 Cookie 存储、下载、脚本执行三条通道 | 中-高（影响产品最核心的差异化承诺：敢用、可审计） | ① **统一外发日志（NFR-SEC-08）先落地**，浏览器随后；② 默认不加载远程内容，导航一律由用户显式发起；③ 独立数据存储，不共享 SQL 客户端凭据与 Cookie；④ 下载与本地文件访问沿用授权目录；⑤ 不向页面注入任何客户端脚本桥（不把数据库能力暴露给网页） |\n"
once("| R-34 | ", R35 + "| R-34 | ", "§9 R-35 浏览器风险")

p.write_text(t, encoding="utf-8")
print("\n📄 SRS 主体已更新（索引、计数、变更记录待下一步）")
