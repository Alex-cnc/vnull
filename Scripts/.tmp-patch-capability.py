# -*- coding: utf-8 -*-
"""一次性补丁：能力规划说明书 —— 计数、映射表、内嵌浏览器专项、变更记录。跑完即删。"""
import pathlib

p = pathlib.Path("Docs/产品能力规划说明书.md")
t = p.read_text(encoding="utf-8")


def once(old: str, new: str, label: str) -> None:
    global t
    assert t.count(old) == 1, f"{label}: 命中 {t.count(old)} 次"
    t = t.replace(old, new, 1)
    print(f"✅ {label}")


# ---------- 1) 派生计数 ----------
once("**164 条需求：108 ✅ / 22 🟡 / 34 ⬜**", "**165 条需求：108 ✅ / 22 🟡 / 35 ⬜**", "能力地图计数 164 → 165")
once("## 3. 差距的主题聚类（⬜ 34 项 + 🟡 中的真实缺口）", "## 3. 差距的主题聚类（⬜ 35 项 + 🟡 中的真实缺口）", "§3 标题 ⬜ 34 → 35")
once("| **FR-EDIT** SQL 编辑与执行体验 | 每天都要用，最该打磨 | 33 | 29 | 1 | 3 | **强** |",
     "| **FR-EDIT** SQL 编辑与执行体验 | 每天都要用，最该打磨 | 34 | 29 | 1 | 4 | **强** |",
     "§2 FR-EDIT 行 33 → 34")
once("含 FR-DRV-09 / NFR-COMP-05 与本次新登记的 FR-AI-13 ~ 15）",
     "含 FR-DRV-09 / NFR-COMP-05、FR-AI-13 ~ 15 与本次新登记的 **FR-EDIT-34（内嵌浏览器）+ NFR-SEC-08（统一外发日志）**）",
     "§2 数据来源说明")

# ---------- 2) 新增专项小节：内嵌浏览器与外发日志（放在第 4 节之后） ----------
SECTION = """
---

## 4.5 内嵌浏览器与统一外发日志（2026-09-23 拍定）

需求提出者在同一次对话里拍了两件事，**顺序本身就是决定**：

| 项 | 拍定内容 | 登记 |
|---|---|---|
| 统一外发日志 | **先做**。所有离开本机的请求（模型调用 / 浏览器导航与子资源 / 外部程序 / 更新检查）写进**同一份**可查看、可导出、可清空的日志 | **NFR-SEC-08** |
| 内嵌浏览器 | 做**完整浏览器**（不是只读预览），放在**编辑器区**（与 SQL 页签同级，**不是**下方面板里的一个页签） | **FR-EDIT-34** |

**为什么"先日志后浏览器"是对的**：浏览器是本产品**第一个通用出网入口** —— 在此之前，产品的
"默认零外发"可以靠"智能体总开关默认关闭"来保证；一旦能加载任意网页，这条承诺就需要**可观测**才能成立。
把外发日志放在前面，等于先给这条承诺装上计量表，再开门。见 R-35（浏览器的新攻击面与外发面）。

**行为契约（两侧一致，见需求书 §10.9 P-14）**：默认不加载远程内容、导航一律由用户显式发起、
出网全部进统一外发日志、独立数据存储（不共享 SQL 客户端的凭据与 Cookie）、下载走授权目录、
**不向页面注入任何客户端脚本桥**（不把数据库能力暴露给网页）。

**放在编辑器区而不是下方面板**的理由：下方面板在 FR-EDIT-30 定稿时被明确为**与产品未来解耦的应用级面板**
（问题 / 输出 / 终端 / 调试控制台四种通用页签），而浏览器是**任务性**的：
它服务于"边查资料边写 SQL"，与 SQL 页签同处一区才能并排、切换、随会话恢复。
"""
once("\n---\n\n## 5. 依赖、阻塞与前置决策", SECTION + "\n---\n\n## 5. 依赖、阻塞与前置决策",
     "新增 §4.5 内嵌浏览器与统一外发日志")

# ---------- 3) 映射表 ----------
once("| **双平台等价** | **P7；SRS §0.8 / §10.9 / §10.10；概要设计 §3（本次建立）** | Linux 侧适配层实现（另一仓库，按契约推进） |",
     "| **双平台等价** | **P7；SRS §0.8 / §10.9 / §10.10；概要设计 §3（本次建立）** | Linux 侧适配层实现（另一仓库，按契约推进） |\n"
     "| 统一外发日志 | **NFR-SEC-08（本次登记，先做）** | — |\n"
     "| 内嵌浏览器 | **FR-EDIT-34（本次登记，等 NFR-SEC-08）** | 浏览器内直接操作数据库 / 页内 AI 助手（均未讨论，不登记） |",
     "§7 映射表新增两行")

# ---------- 4) 前置决策表：补一条 ----------
once("| D6 |", "| D6 |", "（占位，确认 D6 存在）") if "| D6 |" in t else print("· 跳过 D6（格式不同）")

# ---------- 5) 变更记录 ----------
CHANGELOG = (
    "| **v1.10** | 2026-09-23 | **内嵌浏览器与统一外发日志拍定（新增 §4.5）+ 评审高危缺陷修复后的能力重派生**。"
    "① 需求提出者拍定两件事，**顺序即决定**：先做**统一外发日志**（→ NFR-SEC-08），再做**完整浏览器**"
    "并放**编辑器区**（→ FR-EDIT-34）；② 新增 §4.5 记下拍定内容、顺序理由（浏览器是第一个通用出网入口，"
    "“默认零外发”从此需要**可观测**才能成立）、行为契约与「为什么放编辑器区而不是下方面板」"
    "（下方面板在 FR-EDIT-30 已明确为与产品未来解耦的应用级面板）；③ §7 映射表新增两行；"
    "④ 能力地图按 SRS 重派生：**164 → 165 条：108 ✅ / 22 🟡 / 35 ⬜**（FR-EDIT 33 → 34）。"
    "**同日代码侧**：评审确认的高危缺陷 R-29 / R-30 / R-32 / R-34 **已修复**、R-31 / R-33 **部分修复**"
    "（逐条写明未做部分）；**顺带测出文档漂移** —— 小节标题的「（N 条）」无人校验，§3.2 写 44 实际 50、"
    "§3.10 写 8 实际 9，已改正并加进 `check-doc-tables.py`。**verify-core.sh 760 → 776 tests / 0 failures；"
    "verify-all.sh 七项全过** | 鲸鱼娘（开发助理） |\n"
)
once("| 版本 | 日期 | 变更摘要 | 作者 |\n|---|---|---|---|\n",
     "| 版本 | 日期 | 变更摘要 | 作者 |\n|---|---|---|---|\n" + CHANGELOG, "§9 变更记录 v1.10")

p.write_text(t, encoding="utf-8")
print("\n📄 能力规划已更新")
