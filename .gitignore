# 本地口令文件（开发形态：MD5 派生密钥的可逆混淆，**绝不入库**）
.secrets/

# 构建产物
.build/
.build-cache/
DerivedData/
dist/
.swiftpm/

# 本地备份与系统文件
.backups/
.DS_Store

# 跨项目转移稿（属于灵感收集器，不属于本仓库）
_transfer_*.md

# Xcode 个人状态（不应入库：含用户名与本地路径）
xcuserdata/
*.xcuserstate

# Docs：对外发布的文档 + 三书（需求规范书 / 产品能力规划说明书 / 概要设计）。
#
# 三书必须入库：macOS 与 Linux 两个平台以它们为**唯一共享事实源**（SRS §0.8），
# 靠 GitHub 同步版本。其余内部工程文档（调研 / 测试 / 发布 / 评审 / 过程记录）
# 仍留在本地，不随仓库发布。新增对外文档时在这里加白名单。
/Docs/*
!/Docs/README.md
!/Docs/功能清单（一页纸）.md
!/Docs/功能清单（管理视图）.md
!/Docs/需求规范书.md
!/Docs/产品能力规划说明书.md
!/Docs/概要设计.md
!/Docs/平台实现状态.json
# 设计说明（浏览器页签等）也属于交接资料，随三书一起入库
!/Docs/design/
!/Docs/design/*.md

# 图标中间产物（可由 Scripts/make-app-icon.swift 从 AppIcon-source.png 重新生成）
App/Resources/AppIcon-1024.png

# 并行代理各自使用的独立 SwiftPM 构建目录（避免同一 scratch 的锁竞争），只在本机存在
/.build-agent-*/
/.build-cache-*/
/.build-x86/
/.build-cache-x86/
/.build-ai14/
/.build-cache-ai14/
/.build-ai15/
/.build-cache-ai15/
